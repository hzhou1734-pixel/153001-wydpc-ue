# 2026-09-17 工作日志

## v1.0.52 业务总览补充生活服务业务
- `src/views/dashboard/business/index.vue`：新增「生活服务业务」卡片与表格行（订单数/交易总额/取消订单数量/有效总金额）
- 生活服务为线下交易：交易总额按全部订单金额统计、有效总金额为未取消订单金额合计（无 pay_status 字段）
- 卡片标题旁与表格备注列均标注「系统不收取对应服务金额」；顶部汇总文案与表下方说明均注明合计不含生活服务业务
- 合计行仅统计物业收取的陪诊/托管/膳食三类业务
- 导出 CSV 增加备注列
- 修复：`.tmp-dist` 曾被误提交，已 `git rm --cached` 并在 `.gitignore` 增加 `.tmp-dist/`，amend 后强制推送
- 构建 `npx vite build --outDir=.tmp-dist` 通过（45s）；commit `75b0494` + tag v1.0.52 已 push origin/main

## v1.0.53 待处理事项并入数据总览
- `src/views/dashboard/overview/index.vue`：追加「今日新增」「待审核事项」两个卡片区块（原 todo 页内容整体迁入），数量由 mock 实时统计，点击跳转对应列表
- 删除 `src/views/dashboard/todo/` 页面与目录
- `src/mock/user.ts`：mockMenu 移除「待处理事项」节点，mockPermissions 移除 `dashboard.todo/lists`
- `src/mock/perms.ts`：数据台权限目录仅保留 数据总览/业务总览，rolePerms 同步清理
- 构建 `npx vite build --outDir=.tmp-dist` 通过（74s）；commit `70ceea9` + tag v1.0.53 已 push origin/main

## v1.0.54 小区列表改为单小区资料编辑+运营数据
- 一个物业后台仅管理一个小区：`community/lists` 由列表样式改为单小区资料编辑 + 运营数据概览
- 小区资料卡（门头照/名称/省市区/详细地址/添加时间/状态，弹窗可编辑保存）；运营数据卡（楼栋/总户数/已认证/待审核/员工/状态 + 托管·膳食·陪诊订单统计表含合计行）
- 移除新增/删除/启用停用操作；菜单「小区列表」→「小区信息」
- 权限目录 community.lists 仅保留「编辑资料」，rolePerms/mockPermissions 移除 community.lists/add、community.lists/status
- 构建 `npx vite build --outDir=.tmp-dist` 通过（59s）；commit `4afca20` + tag v1.0.54 已 push origin/main

## v1.0.55 移除小区状态展示
- `src/views/community/lists/index.vue`：资料卡删除「小区状态」标签，运营数据概览卡删除「小区状态」统计块
- `src/views/community/detail/index.vue`：基础信息 descriptions 删除「小区状态」项
- 全局无「小区状态」残留（grep 校验）
- 教训：构建输出经管道截断（head/tail）时 vite 可能被 SIGPIPE 提前终止且退出码取自管道尾部命令，必须重定向到文件再验证 EXIT 码与产物
- 构建 `npx vite build --outDir=.tmp-dist` 通过（68s，344 个资源文件）；commit `cbfd936` + tag v1.0.55 已 push origin/main
