# 2026-09-19 工作日志

## v1.0.56 修复小区信息页运营数据 NaN 与省市区显示
- `src/views/community/lists/index.vue`：`buildOrderStat` 返回字段名（unsettled_amount/settled_amount/cancel_count/cancel_amount）与模板及 bizTotal 汇总所用字段（unsettled/settled/cancelCount/cancelAmount）不一致，spread 后取值全为 undefined → 金额列显示 ¥NaN。统一为后者，表格各列恢复正常
- 省市区显示「—」：mock 地址无省份前缀（如「杭州市西湖区文一西路 128 号」），原正则强制要求以「浙江省|江苏省」开头；改为可选省 + 市 + 区县匹配，现显示「杭州市 / 西湖区」
- 构建 `npx vite build --outDir=.tmp-dist` 通过（84s）；commit `6c63543` + tag v1.0.56 已 push origin/main（推送期间本地代理 127.0.0.1:2849 一度 502、直连超时，重试后恢复）
- Shell 环境 PATH 损坏时的替代方案：git 用绝对路径 `C:/Users/zhou/.workbuddy/binaries/PortableGit/versions/1.2.0/cmd/git.exe`；base64 用 node -e 生成；curl/node 仍可用

## dev 工作分支创建（17:20）
- 用户要求：后续每次改动自动提交到分支上。已从 main（v1.0.56，6c63543）创建并切换到 `dev` 分支，日志变更以 `chore: 更新工作日志` 提交（ff19303）
- 约定已写入 MEMORY.md：后续改动默认提交 dev；main/EdgeOne 保持 v1.0.56，用户要求时再合并
- ~~⚠️ dev 分支推送被网络阻塞~~ → 17:45 代理恢复，`git push -u origin dev --tags` 成功：dev 分支（ff19303 + 8f398c0）与 tag v1.0.57 均已上 GitHub，dev 已跟踪 origin/dev

## v1.0.57 楼栋房号「认证业主」列改为统一展示（dev 分支首个功能提交）
- `src/views/community/building/index.vue`：认证业主列统一 flex 布局——未认证：业主姓名 + 手机号 + 灰色「未认证」标签；已认证：业主头像 + 姓名 + 手机号 + 绿色「已认证」标签（头像支持点击预览）
- mock roomList 未认证记录本身含 owner/phone（如张伟 13812341001），无需改数据
- 构建 `npx vite build --outDir=.tmp-dist` 通过（69s）；commit v1.0.57 + tag 提交于 dev 分支（首次功能提交到 dev，同批推送积压的 ff19303）

## v1.0.58 认证业主列视觉强化（dev）
- `src/views/community/building/index.vue`：认证业主改为状态卡片块——已认证：浅绿底（--el-color-success-light-9）+ 绿色左竖条 + 头像/深色姓名 + 绿标；未认证：浅灰底 + 灰竖条 + 灰化姓名/手机号 + 灰描边标签。姓名与手机号上下两行排布，整列扫视绿/灰块即区分
- 构建 `npx vite build --outDir=.tmp-dist` 通过（47s）；commit v1.0.58 + tag 提交于 dev（推送遇代理抖动 502/schannel 断连，重试至成功）

## v1.0.59 楼栋房号新增认证状态筛选（dev）
- `src/views/community/building/index.vue`：筛选区在「楼栋/房号」后新增「认证状态」下拉（全部/已认证/未认证，clearable）；treeList 中仅过滤房号行，选中状态后无匹配房号的楼栋整行隐藏；重置按钮同步清空该条件
- 与小区/关键词筛选可叠加使用；构建通过（61s）；commit v1.0.59 + tag 提交并推送 dev（同批补推 v1.0.58）

## v1.0.60 添加/编辑房号弹窗补充业主字段（dev）
- `src/views/community/building/index.vue`：房号弹窗在「房号」后新增「业主姓名」「手机号码」两个非必填字段（placeholder 注明未登记可留空）；手机号填写时校验 11 位（^1\d{10}$），maxlength=11
- 编辑回填时 mock 中 '-' 占位转为空串，保存时空值回落为 '-'；新增房号使用表单值替代原先固定 '-'
- 认证状态不受影响（仍为未认证，认证走认证审核流程）；构建通过（46s）；commit v1.0.60 + tag 提交于 dev

## v1.0.61 用户列表字段调整（dev）
- `src/views/consumer/lists/index.vue`：删除「未结算账单总额」「已结算账单总额」两列；手机号码后新增「顾好家币」列（取 data_user.ts coinAccounts[id].balance）与「已完成订单总金额」列（按用户 mobile 从三类订单实时统计 status=3 且 pay_status=1 的 amount 合计，符合 mock 实时计算约定）
- consumerList 中遗留的 unsettled_amount/settled_amount 字段仅剩 mock 数据定义（grep 校验无页面引用），暂不清理；构建通过（50s）
- ✅ 18:45 代理恢复：v1.0.58～v1.0.61 四个版本 + 4 个 tag 一次性推送 dev 成功（bc4d8f7..be16913），积压清零
- 备注：本日 Bash 工具 PATH 异常（grep/tail/dirname 找不到命令），构建与 git 操作改用 PowerShell 执行

## v1.0.62 员工角色体系切换：楼栋管理员/保安/保洁（dev）
- `src/mock/data.ts`：staffList 11 人重分配为 3 类角色（楼栋管理员 role_id=1：周建国1/2栋、孙丽华3栋、马长顺5/6栋、陈美玲2栋、徐国强2栋；保安 role_id=2：郑海涛、何秀芳、李文斌；保洁 role_id=3：吴秀兰、郭敏、田雅静）；保安/保洁 buildings '-'。修复何秀芳 role 字段乱码（U+FFFD）为「保安」。staffEarnings 删除 escort/delivery/nursing_income 三字段、改为 total_income
- `src/views/staff/lists/index.vue`：roleOptions 3 类；role_id===4 判断全部改 role_id===1（楼栋必选校验/同小区唯一校验/buildingStr）；唯一校验按「小区+楼栋」；角色标签色 primary/success/warning；负责楼栋表单非楼栋管理员禁用并提示规则；「修改角色」弹窗复用 roleOptions 自动适配
- `src/mock/config.ts`：staff_role 字典 → 楼栋管理员1/保安2/保洁3
- `src/views/community/building/index.vue`：楼栋管家查找条件改为 role_id===1 且同小区且楼栋匹配
- `src/views/finance/earnings/index.vue`：删「陪诊/配送/托管收益」三列与导出列，仅留「单次收益总金额」；alert 说明简化；收益计算保留（趟次×配置单价，角色与服务解耦）
- `src/mock/api.ts`：getStaffEarnings 的 total_income 改为直接取 staffEarnings.total_income（原 escort/delivery/nursing_income 相加已失效）；删除无页面引用的 addStaffEarning；财务概况 staff_earnings_total 汇总同步改 total_income（漏改会导致 NaN）
- 三个订单派单页（order/nursing、order/meal、order/escort）：staffOptions 从按 role_id 过滤改为全部员工；下拉选项「姓名（角色 · 小区）」；label「指派托管员/配送员/陪诊员」→「指派员工」；校验与 alert 文案同步（v1.0.36 的角色绑定派单约定已废止）
- 构建：`npx vite build --outDir=.tmp-dist` 通过（直接 node vite.js；PS 5.1 下 2>&1 管道会中断 vite，重定向用 Start-Process 被禁，node 直跑最稳）；commit 15ee3b9 + tag v1.0.62 推送 dev 成功
- ⚠️ 新坑：cmd/git.exe 与 mingw64/bin/git.exe 均报 `remote-https is not a git command`（PortableGit 解包目录 mingw64/libexec/git-core 缺失）→ 解决：`git --exec-path="<PortableGit>/mingw64/bin"`（git-remote-https.exe 在 bin 下）

## v1.0.63 添加/编辑员工弹窗删除「所属小区」字段（dev）
- `src/views/staff/lists/index.vue`：删除弹窗「所属小区」下拉项；「负责楼栋」从 el-select（依赖所属小区联动过滤）改为 el-cascader 多选（小区 → 楼栋两级分组，值路径 [小区名, 楼栋名]，collapse-tags），不再依赖所属小区字段
- community 保存逻辑：楼栋管理员取第一个所选楼栋的小区名；保安/保洁沿用编辑原值、新增兜底 communityOptions[0]；communityIdMap/buildingOptions/onCommunityChange 已移除
- 楼栋唯一校验升级为逐个「小区+楼栋」路径比对，冲突提示含具体小区与楼栋（如「XX小区 2栋 已被管理员『张三』负责」）
- 编辑回填：row.buildings.split('、') 与 row.community 组合为路径数组；构建通过（47s，首次跑挂一次 exit 1 为瞬时故障，重跑即过）；commit d2bf57a + tag v1.0.63 已推送 dev

## v1.0.64 托管服务移除「半天价格/整天价格」说法（dev）
- 用户明确要求：托管服务没有半天价格/整天价格的说法（注意：需求文档第 87/90 行原文本有此说法，但用户口径优先，已更新 MEMORY.md 约定）
- `src/mock/data_service.ts`：NursingItem 类型 half_price/day_price → 单一 price；8 条托管服务数据改为 price（取原整天价 day_price 作为服务价格）
- `src/views/service/nursing/index.vue`：列表「半天价格/整天价格」两列 → 一列「价格」；弹窗表单两项 → 一项「价格」（必填）；editForm/openAdd/openEdit/submitEdit 同步
- ⚠️ 陪诊服务（EscortItem + service/escort 页面）的半天/整天价格保留不动——其预约口径为「仅支持半天/整天预约」，用户仅要求托管服务
- data.ts 的 nursingServices（api.ts getNursingServiceList 用）本就是单一 price，未动；该 API 无页面引用
- 构建通过（46s）；commit 6a8f95f + tag v1.0.64 本地已提交，**推送被代理 502 阻塞**（19:40 起 schannel 断连/Empty reply/502 交替，共 10 次失败）；本地积压待补推：58487c0（chore 日志 v1.0.63）+ 6a8f95f（v1.0.64）+ tag v1.0.63、v1.0.64（d2bf57a v1.0.63 已在远端）

## v1.0.65 物业钱袋子结余金额改为手工输入（dev）
- 用户要求：小区钱袋子（finance/wallet 菜单，含物业/托管/膳食三个 Tab）结余金额给输入框，不做自动计算
- `src/views/finance/wallet/index.vue`：物业钱袋子添加弹窗「结余金额」从只读自动计算（收入-支出）改为 el-input-number 手工输入（允许负数、两位小数）；form 增加 balance_amount；openAdd 重置；submitAdd 保存 money(form.balance_amount)；computedBalance 计算属性删除
- `src/mock/data_finance.ts`：buildPropertyWallet 不再计算 balance_amount，4 条 mock 数据直接给 balance_amount 字面值（22740/14480/10550/16560.00）；文件头注释同步（仅托管/膳食预收金额保留自动计算）
- 托管/膳食钱袋子的「预收金额 = 订单数量 × 每单单价」自动计算未动（用户未要求）
- 构建通过（57s）；commit v1.0.65 + tag 本地已提交，推送仍被代理 502 阻塞（累计 17 次失败）；本地积压：58487c0、6a8f95f、ce15437、v1.0.65 提交 + tag v1.0.63/64/65

## v1.0.66 托管/膳食钱袋子每单单价改为自动计算（dev）
- 用户要求：托管单、膳食单每单单价是自动算出来的，不是手动输入
- `src/views/finance/wallet/index.vue`：托管/膳食钱袋子表单——「预收金额」改为输入框（手工输入，允许负数？否，:precision=2 无 min 限制），「每单单价」改为只读自动计算 = 预收金额 ÷ 订单数量（订单数量为 0 时按 0 显示）；字段顺序调整为 订单数量 → 预收金额 → 每单单价 → 成本金额；form.unit_price → form.prepay_amount；submitAdd 保存 prepay_amount 手工值 + unit_price 计算值
- `src/mock/data_finance.ts`：buildBusinessWallet 改为 raw 给 prepay_amount、unit_price 自动计算（预收÷订单数）；6 条数据换算（托管 14880/17120/11880、膳食 14880/16256/11880，单量不变单价结果与原一致）；文件头注释同步
- 物业钱袋子结余手工输入口径不变（v1.0.65）
- 至此钱袋子三个 Tab 计算口径统一：物业——收入/支出/结余全部手工；托管/膳食——订单数量/预收金额/成本手工，每单单价自动
- 构建通过（44s）；**代理 20:15 左右恢复**，积压全部补推成功：58487c0/6a8f95f/ce15437/03b52e5/v1.0.66 提交 + tag v1.0.63/64/65/66 全部上远端（remote dev HEAD = 9af2115）

## 20:23 用户重复反馈「膳食单每单单价自动算」确认
- 全局 grep 复核：wallet/index.vue 托管/膳食共用弹窗（template v-else）的每单单价已是只读自动计算（预收÷订单数量），列表列显示 mock 计算值，order/meal、账单结算等页面无「每单单价」字段——v1.0.66 已完整覆盖膳食，无需改代码
- 判断为用户浏览器未刷新/HMR 未生效，提示强刷；无代码变更、无新提交

## v1.0.67 员工收益列表：删单次收益总金额，改待结算/已结算总收益（dev）
- `src/mock/data_finance.ts`：earningBase 趟次按结算状态拆分 done/pending 两套字段（nursing/delivery/escort × done/pending，总量与原 count 一致，done 占大头）；staffEarningRows 透传 6 字段
- `src/views/finance/earnings/index.vue`：删「单次收益总金额」列；新增「待结算总收益」（橙，pending 趟次×配置单价）与「已结算总收益」（绿，done 趟次×配置单价）；列宽全部改 min-width 弹性（180/140/110/150/150/160）解决疏密不均；导出列与 alert 说明同步
- `src/views/finance/overview/index.vue`：财务概况 staff_earnings_total 汇总同步 (done+pending) × 单价
- ⚠️ 首次构建失败：新表格块自带 `</el-table>` 结束标签与旧模板残留重复 → Invalid end tag，删除重复行后通过（59s）
- 无关项：community/detail 的 nursing_count（小区详情统计）、api.ts/data.ts 的 staffEarnings.total_income（无页面引用的旧接口链）均未动；commit d4906fa + tag v1.0.67 已推送 dev

## v1.0.68 财务概况补充生活帮手单（线下支付口径）（dev）
- 用户要求：财务概况中补充显示生活帮手单，同时备注这类订单平台不做支付，由用户线下支付
- `src/views/finance/overview/index.vue`：
  - 新增 `buildHelperStat`：生活帮手单不校验 pay_status（mock 中 helperOrders 无该字段，平台不做支付），仅按订单状态统计——有效=未取消(status≠4)、待结算=有效且未完成、已结算=status 3；退款/取消口径与其他单一致（status 4）
  - allOrders 追加 helperOrders（`@/mock/data_order`）映射 `_type: '生活帮手单'` + `_offline: true`；typeRows 数组加「生活帮手单」，该行 offline 标记
  - 两张表格「订单类型」列（replace_all）改为 template：行名后 el-tag 警告色描边「线下支付」（min-width 120→150）；summary 汇总 valid 过滤按 `_offline ? status!==4 : pay_status===1&&status!==4` 分流，顶部三大统计与待/已结算总额均含生活帮手单，口径与明细行合计一致
  - 口径文案：订单金额卡「有效订单 = 已支付且未取消（生活帮手单平台不做支付，由用户线下支付，按未取消订单计）…」；结算金额卡补「生活帮手单平台不做支付，由用户线下支付」
  - 生活帮手单不计入员工收益（收益配置只覆盖托管/膳食/陪诊三类）
- mock 数据源：`src/mock/data_order.ts` helperOrders 10 条（120/80/160/160/260/30/68/480/150/100，状态 2/3/1/3/2/1/0/3/4/1）
- 构建 43s 通过（首跑 exit 1 为已知瞬时故障，重跑即过；本次且伴随 EAP=Stop 下 2>&1 捕获中断——补救：命令首行显式 `$ErrorActionPreference='Continue'`）；commit de9237c + tag v1.0.68 一次推送成功
- ⚠️ 环境新坑：本会话 PowerShell 工具的 Remove-Item 被 safe-delete 包装为移入回收站且对含中文路径的项目目录报 trash-failed（实际部分成功/部分失败，不可靠）；bash shim 缺 coreutils（tr/base64/sleep/tail/rm 均不可用）；删除文件改用 `node -e "fs.unlinkSync(...)"` 最可靠。git commit/push 改走 Bash 工具（git.exe 绝对路径 + exec-path），bash 环境下 git 本身可用

## v1.0.69 Banner 跳转改为「类目 + 具体内容」二级选择（dev）
- 用户要求：banner 跳转需补充类目——托管、陪诊、生活帮手、活动、钱袋子、精彩内容、通知公告；选类目后再选具体内容（哪个托管服务/陪诊服务/精彩内容详情等）；钱袋子特殊，固定跳指定页面
- `src/views/article/banner/index.vue`：
  - linkTypes 改为 8 项：不跳转 + 托管服务/陪诊服务/生活帮手/社区活动/钱袋子/精彩内容/通知公告（旧「内置页面/自定义链接」删除）
  - 新增 linkPathMap（类目→链接前缀 /pages/nursing|escort|helper|activity|article|notice/detail）与 linkContentMap（类目→二级选项）：托管=data_service nursingServices（1001+，label 带 type）、陪诊=escortServices（2001+）、生活帮手=helperServices（4001+，title）、活动=contentActivityList（7001+）、精彩内容=contentWonderfulList（2001+）、通知公告=contentNoticeList（3001+）——均与对应管理页数据同源
  - 钱袋子 WALLET_LINK=/pages/wallet/index：选中后显示禁用输入框+说明「固定跳转 APP 钱袋子页面，无需选择内容」
  - buildLink 按 link_type+link_id 生成 `前缀?id=N`；parseLink 反向解析（前缀匹配+id 提取）用于 openEdit 回填；submitEdit 校验「请选择XX」并保存 link/link_type/link_id 三字段
  - 列表新增「跳转设置」列（linkDesc：类目 · 内容名，如「托管服务 · 幼儿全日托（1.5-3岁）」；钱包固定页；空=不跳转）；editForm 删 link 字段
- `src/mock/data_content.ts` contentBannerList：8 条数据补 link_type/link_id 字段；旧列表页链接改为详情链接（8002 托管→1001、8003 陪诊→2001、8004 帮手→4001）；8005 由膳食（新类目无膳食）改为钱袋子示例「首页-钱袋子便捷缴费」；新增 8007 精彩内容示例（2001）、8008 通知公告示例（3001）
- 构建通过（58s）；commit b36c4dc + tag v1.0.69 一次推送成功（dev: de9237c→b36c4dc）

## v1.0.70 报名活动删除「报名总人数」设置（dev）
- 用户要求：添加报名活动时删除报名总人数的设置
- `src/views/article/activity/index.vue`：
  - 添加/编辑弹窗删除「报名总人数」必填项（el-input-number）；editForm 删 limit 字段（openAdd 默认值 100、openEdit 回填、submitEdit「请设置报名总人数」校验同步删除）
  - 列表「报名人数/总人数」列（signup / limit）改为「报名人数」列（仅 signup + 「人」）；详情弹窗「报名人数」改为「N 人」
- `src/mock/data_content.ts`：contentActivityList 8 条记录删除 limit 字段（200/30/120/48/80/150/60/40）
- 无关项：src/mock/data.ts 的旧 activityList（limit 字段仍在）仅被 consumer/detail 做 title/time 反查、api.ts 旧接口用，无页面渲染 limit，未动
- 构建通过（43s）；commit eb2bcd1 + tag v1.0.70 本地已提交，推送遇代理 502（21:17 起，连续 6 次失败），后台 30s 间隔重试循环第 15 次于 21:35 恢复推送成功

## v1.0.71 人才详情补充显示认证申请表单内容（dev）
- 用户要求：人才库-人才详情中补充显示用户提交人才申请时提交的表单详细内容
- `src/views/article/talent/index.vue`：人才详情抽屉在「基础信息」与「订单信息」之间新增「认证申请信息」区块——按手机号从 hrCertList 匹配该人才的认证申请（talentCerts computed，可多条），展示认证技能标题/技能类目/审核状态（el-tag，待审核/已通过/已驳回）/提交时间/审核时间/驳回备注（仅驳回时）/详情描述/认证凭证图片（可预览）；无记录时显示「（该人才暂无认证申请记录）」
- `src/mock/data_content.ts` 两处数据一致性修正：
  - hrCertList 5 条记录的手机号/头像与 talentList 同人对齐（钱志明→13912343001/talentImg(1)、赵有发→003/3、蒋伟→004/4、王晓梅→002/2、何秀娟→005/5）——此前同人两个手机号（138123410xx vs 139123430xx），违反「同一人手机号一致」约定；审核通过进人才库的匹配逻辑（按 mobile）因此能命中种子人才
  - 新增 6009 罗小川/6010 孙丽/6011 邓国平 三条已通过认证申请，使人才库 8 人全部有申请记录；张桂芳/孙丽华/周凯（待审核/已驳回、非人才）保持用户号段与 userImg 头像
- 构建通过（43s）；commit e9850d4 + tag v1.0.71 一次推送成功（dev: eb2bcd1→e9850d4）

## v1.0.72 订单评价新增显示/隐藏设置（dev）
- 用户要求：订单评价加一个显示隐藏的设置
- `src/views/order/evaluate/index.vue`：
  - 筛选区在「评星」后新增「显示状态」下拉（全部/显示/隐藏）；doFilter 支持 status 过滤
  - 列表在「评价时间」后新增「显示状态」列（el-switch，1 显示 / 0 隐藏，toggleShow 提示「已设置为显示」/「已隐藏，前端不再显示该评价」）——遵循 v1.0.41 状态列开关约定
- `src/mock/data_order.ts`：orderEvaluations 12 条补 status 字段（5010 两星、5012 一星默认隐藏，其余显示）
- 构建通过（47s）；commit baf34f4 + tag v1.0.72 一次推送成功（dev: e9850d4→baf34f4）

## v1.0.73 生活帮手订单操作列一行显示（dev）
- 用户反馈：生活帮手订单操作功能自动换行有样式问题
- `src/views/order/helper/index.vue`：操作列 3 个 `size="small"` 普通按钮（订单关联/取消订单/详情）改为项目通用的 link 按钮（订单关联 link primary、取消订单 link danger、详情 link primary，保留 canRelate/canCancel 禁用逻辑），列宽 190→200，确保单行容纳
- 构建通过（42s）；commit ca229e9 + tag v1.0.73 一次推送成功（dev: baf34f4→ca229e9）

## v1.0.74 生活帮手订单说明文案改为状态快捷筛选（dev）
- 用户要求：删除列表卡片头部的「说明：生活帮手订单无需线上派单…」文案，原位置补充订单状态快捷筛选（全部/待派单/进行中/已完成/已取消）
- `src/views/order/helper/index.vue`：header 说明 span 删除，改为 el-radio-group(el-radio-button) 绑定 queryParams.status（进行中=status 2），@change=resetPage()；与筛选表单里的订单状态下拉双向同步，重置按钮自动回到「全部」
- 注意：表格状态列 tag 仍显示「服务中」（与托管/膳食/陪诊订单页及文档枚举一致），快捷筛选用「进行中」为用户指定文案，尚未统一
- 构建通过（43s）；commit afcab5c + tag v1.0.74 一次推送成功（dev: ca229e9→afcab5c）

## v1.0.75 陪诊订单卡片标题改为状态快捷筛选（dev）
- 用户要求：删除列表卡片头部「陪诊订单」标题文字，原位置补充订单状态快捷筛选（全部/待派单/进行中/已完成/已取消）
- `src/views/order/escort/index.vue`：header 的 card-title span 删除，替换为 el-radio-group(el-radio-button) 绑定 queryParams.status（进行中=status 2），@change=resetPage()；与筛选表单订单状态下拉同步
- 构建通过（43s，工具层偶发 exit 1 但日志确认 ✓ built）；commit 22d4813 + tag v1.0.75 推送成功（代理先 8 连败 502/schannel，后台重试循环第 1 次即恢复成功，dev: afcab5c→22d4813）
- 模式提醒：用户正在逐页给订单列表加状态快捷筛选，托管(nursing)/膳食(meal) 页大概率会提同样需求，header 结构相同（title span 或 title+说明 span），照 v1.0.74/75 模式处理即可

## v1.0.76 派单/订单关联限定本小区人员（dev）
- 用户要求：所有派单所选的指派员工都应是订单同小区的，不要出现不同小区名称
- `order/escort|meal|nursing/index.vue`：staffOptions 由全量 staffList 改为 computed 按订单 community 过滤；下拉选项 label 去掉「· 小区名」后缀，只保留「姓名（角色）」
- `order/helper/index.vue` 订单关联：talentOptions 改 computed 按 relateRow.community 过滤；弹窗表格删除「所属小区」列（该列原 prop=community 在 talentList 无此字段时本就显示为空）
- `mock/data_content.ts`：TalentItem 接口新增 community 字段，8 个人才补小区（钱志明/王晓梅→颐景园·江南里、赵有发→绿城·桂语江南、蒋伟/罗小川→万科·未来城三期、何秀娟→保利·天悦湾、孙丽/邓国平→保利·天悦湾/绿城·桂语江南），确保 helperOrders 涉及的 4 个小区均有 status=1 在职人才可选（注意 mobile+skill 两行组合才在 data_content.ts 唯一——hrCertList 同手机号需更多上下文区分）
- 数据核对：staffList 覆盖 escort/meal/nursing 订单涉及的全部 4 小区（颐景园2/绿城2/保利4/万科2 可用），无空下拉风险
- 构建通过（44s）；commit 22e5b6d + tag v1.0.76 一次推送成功（dev: 22d4813→22e5b6d）

## v1.0.77 膳食订单自取单隐藏派单入口、移除待支付状态（dev）
- `order/meal/index.vue`：
  - 操作列「派单」按钮加 `v-if="row.need_delivery === 1"`，自取单不再显示派单入口（配送单仍保留灰色禁用态）
  - statusMap 移除 0 待支付 → {1待派单/2服务中/3已完成/4已取消}；派单弹窗「配送要求」简化为直接显示 delivery_time，删除不可达的「无需配送」警示 alert
- `mock/data.ts`：mealOrders 唯一 status 0 记录（2005 刘洋自取单）改为 status 4 已取消，日志改为「用户提交膳食订单后未支付，订单已自动取消」
- 注意：escort/nursing 页仍保留待支付状态（用户只提膳食），支付状态列（已支付/未支付）与订单状态是两回事未动
- 构建通过（43s）；commit a9fe8d4 + tag v1.0.77 推送成功（代理 5 连败后第 6 次成功，dev: 22e5b6d→a9fe8d4）

## v1.0.78 膳食订单卡片标题改为状态快捷筛选（dev）
- 用户框选「膳食订单」标题要求删除并在原位加订单状态快捷筛选（全部/待派单/进行中/已完成/已取消）——与 v1.0.75 陪诊页同模式
- `order/meal/index.vue`：header card-title 换成 el-radio-group 绑定 queryParams.status；meal 页原本无 status 查询参数，queryParams 补 `status: '' as '' | number`，doFilter 补 status 过滤分支；重置按钮经 resetParams 自动回到「全部」
- 构建通过（43s）；commit 614d338 + tag v1.0.78 一次推送成功（dev: a9fe8d4→614d338）
- 状态快捷筛选进度：helper(v1.0.74)、escort(v1.0.75)、meal(v1.0.78) 已加；nursing（托管订单）未加，用户提出时照同模式处理（nursing header 为 title+说明 span 结构，参照 helper 保留标题只换说明）

## v1.0.79 膳食订单详情新增送达确认图片（dev）
- 用户要求：订单确认送达的位置补充送达图片显示
- `order/meal/index.vue` 详情弹窗在「地址信息」与「金额信息」之间新增「送达确认」区块：v-if delivery_imgs 有值时渲染图片列表（el-image 可点击预览，initial-index 定位）
- `mock/data.ts`：3 条已送达订单（SC20260907002/SC20260905004/SC20260901008，均 status 3）补 delivery_imgs 字段（picsum ghj-meal-deliver-N/400/300，各 1~2 张）；未送达/自取/取消单无此字段不显示区块
- 查数据时注意：mealOrders 单行对象里 `pay_status` 在前，正则 `/status: (\d)/` 会误匹配 pay_status，需用 `/, status: (\d)/`
- 构建通过（42s）；commit 2809444 + tag v1.0.79 一次推送成功（dev: 614d338→2809444）

## v1.0.80 托管订单标题说明改为状态快捷筛选（dev）
- 用户框选「托管订单」标题+「说明：用户可能提前多天下单…」整段要求删除，原位加订单状态快捷筛选（全部/待派单/进行中/已完成/已取消）
- `order/nursing/index.vue`：header 左侧 title+说明 span 整体替换为 el-radio-group 绑定 queryParams.status（该页已有 status 查询参数与筛选下拉，无需加字段），右侧「导出Excel」按钮保留
- 状态快捷筛选四订单页全部完成：helper(v1.0.74)、escort(v1.0.75)、meal(v1.0.78)、nursing(v1.0.80)

## v1.0.81 托管订单移除待支付状态（dev）
- 同 v1.0.77 膳食模式：`order/nursing/index.vue` statusMap 移除 0 待支付 → {1待派单/2服务中/3已完成/4已取消}（筛选下拉选项随之减少）
- `mock/data.ts`：nursingOrders 两条 status 0 记录（TG20260905004 刘洋、TG20260901008 李娜）改为 status 4 已取消，日志「用户提交托管订单后未支付，订单已自动取消」
- 现状：meal/nursing 均无待支付；escort 仍保留待支付（用户未提）
- 构建通过（41s）；commit 4f3f6d6 + tag v1.0.81 推送成功（代理 3 连败后第 4 次成功，dev: 4c176f3→4f3f6d6）
- 构建通过（42s）；commit 4c176f3 + tag v1.0.80 一次推送成功（dev: 2809444→4c176f3）
