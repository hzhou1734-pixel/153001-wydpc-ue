# 2026-09-19 工作日志

## v1.0.56 修复小区信息页运营数据 NaN 与省市区显示
- `src/views/community/lists/index.vue`：`buildOrderStat` 返回字段名（unsettled_amount/settled_amount/cancel_count/cancel_amount）与模板及 bizTotal 汇总所用字段（unsettled/settled/cancelCount/cancelAmount）不一致，spread 后取值全为 undefined → 金额列显示 ¥NaN。统一为后者，表格各列恢复正常
- 省市区显示「—」：mock 地址无省份前缀（如「杭州市西湖区文一西路 128 号」），原正则强制要求以「浙江省|江苏省」开头；改为可选省 + 市 + 区县匹配，现显示「杭州市 / 西湖区」
- 构建 `npx vite build --outDir=.tmp-dist` 通过（84s）；commit `6c63543` + tag v1.0.56 已 push origin/main（推送期间本地代理 127.0.0.1:2849 一度 502、直连超时，重试后恢复）
- Shell 环境 PATH 损坏时的替代方案：git 用绝对路径 `C:/Users/zhou/.workbuddy/binaries/PortableGit/versions/1.2.0/cmd/git.exe`；base64 用 node -e 生成；curl/node 仍可用

## dev 工作分支创建（17:20）
- 用户要求：后续每次改动自动提交到分支上。已从 main（v1.0.56，6c63543）创建并切换到 `dev` 分支，日志变更以 `chore: 更新工作日志` 提交（ff19303）
- 约定已写入 MEMORY.md：后续改动默认提交 dev；main/EdgeOne 保持 v1.0.56，用户要求时再合并
- ~~⚠️ dev 分支推送被网络阻塞~~ → 17:45 代理恢复，`git push -u origin dev --tags` 成功：dev 分支（ff19303 + 8f398c0）与 tag v1.0.57 均已上 GitHub，dev 已跟踪 origin/dev

## v1.0.57 楼栋房号「认证业主」列改为统一展示（dev 分支首个功能提交）
- `src/views/community/building/index.vue`：认证业主列统一 flex 布局——未认证：业主姓名 + 手机号 + 灰色「未认证」标签；已认证：业主头像 + 姓名 + 手机号 + 绿色「已认证」标签（头像支持点击预览）
- mock roomList 未认证记录本身含 owner/phone（如张伟 13812341001），无需改数据
- 构建 `npx vite build --outDir=.tmp-dist` 通过（69s）；commit v1.0.57 + tag 提交于 dev 分支（首次功能提交到 dev，同批推送积压的 ff19303）

## v1.0.58 认证业主列视觉强化（dev）
- `src/views/community/building/index.vue`：认证业主改为状态卡片块——已认证：浅绿底（--el-color-success-light-9）+ 绿色左竖条 + 头像/深色姓名 + 绿标；未认证：浅灰底 + 灰竖条 + 灰化姓名/手机号 + 灰描边标签。姓名与手机号上下两行排布，整列扫视绿/灰块即区分
- 构建 `npx vite build --outDir=.tmp-dist` 通过（47s）；commit v1.0.58 + tag 提交于 dev（推送遇代理抖动 502/schannel 断连，重试至成功）

## v1.0.59 楼栋房号新增认证状态筛选（dev）
- `src/views/community/building/index.vue`：筛选区在「楼栋/房号」后新增「认证状态」下拉（全部/已认证/未认证，clearable）；treeList 中仅过滤房号行，选中状态后无匹配房号的楼栋整行隐藏；重置按钮同步清空该条件
- 与小区/关键词筛选可叠加使用；构建通过（61s）；commit v1.0.59 + tag 提交并推送 dev（同批补推 v1.0.58）

## v1.0.60 添加/编辑房号弹窗补充业主字段（dev）
- `src/views/community/building/index.vue`：房号弹窗在「房号」后新增「业主姓名」「手机号码」两个非必填字段（placeholder 注明未登记可留空）；手机号填写时校验 11 位（^1\d{10}$），maxlength=11
- 编辑回填时 mock 中 '-' 占位转为空串，保存时空值回落为 '-'；新增房号使用表单值替代原先固定 '-'
- 认证状态不受影响（仍为未认证，认证走认证审核流程）；构建通过（46s）；commit v1.0.60 + tag 提交于 dev

## v1.0.61 用户列表字段调整（dev）
- `src/views/consumer/lists/index.vue`：删除「未结算账单总额」「已结算账单总额」两列；手机号码后新增「顾好家币」列（取 data_user.ts coinAccounts[id].balance）与「已完成订单总金额」列（按用户 mobile 从三类订单实时统计 status=3 且 pay_status=1 的 amount 合计，符合 mock 实时计算约定）
- consumerList 中遗留的 unsettled_amount/settled_amount 字段仅剩 mock 数据定义（grep 校验无页面引用），暂不清理；构建通过（50s）
- ✅ 18:45 代理恢复：v1.0.58～v1.0.61 四个版本 + 4 个 tag 一次性推送 dev 成功（bc4d8f7..be16913），积压清零
- 备注：本日 Bash 工具 PATH 异常（grep/tail/dirname 找不到命令），构建与 git 操作改用 PowerShell 执行

## v1.0.62 员工角色体系切换：楼栋管理员/保安/保洁（dev）
- `src/mock/data.ts`：staffList 11 人重分配为 3 类角色（楼栋管理员 role_id=1：周建国1/2栋、孙丽华3栋、马长顺5/6栋、陈美玲2栋、徐国强2栋；保安 role_id=2：郑海涛、何秀芳、李文斌；保洁 role_id=3：吴秀兰、郭敏、田雅静）；保安/保洁 buildings '-'。修复何秀芳 role 字段乱码（U+FFFD）为「保安」。staffEarnings 删除 escort/delivery/nursing_income 三字段、改为 total_income
- `src/views/staff/lists/index.vue`：roleOptions 3 类；role_id===4 判断全部改 role_id===1（楼栋必选校验/同小区唯一校验/buildingStr）；唯一校验按「小区+楼栋」；角色标签色 primary/success/warning；负责楼栋表单非楼栋管理员禁用并提示规则；「修改角色」弹窗复用 roleOptions 自动适配
- `src/mock/config.ts`：staff_role 字典 → 楼栋管理员1/保安2/保洁3
- `src/views/community/building/index.vue`：楼栋管家查找条件改为 role_id===1 且同小区且楼栋匹配
- `src/views/finance/earnings/index.vue`：删「陪诊/配送/托管收益」三列与导出列，仅留「单次收益总金额」；alert 说明简化；收益计算保留（趟次×配置单价，角色与服务解耦）
- `src/mock/api.ts`：getStaffEarnings 的 total_income 改为直接取 staffEarnings.total_income（原 escort/delivery/nursing_income 相加已失效）；删除无页面引用的 addStaffEarning；财务概况 staff_earnings_total 汇总同步改 total_income（漏改会导致 NaN）
- 三个订单派单页（order/nursing、order/meal、order/escort）：staffOptions 从按 role_id 过滤改为全部员工；下拉选项「姓名（角色 · 小区）」；label「指派托管员/配送员/陪诊员」→「指派员工」；校验与 alert 文案同步（v1.0.36 的角色绑定派单约定已废止）
- 构建：`npx vite build --outDir=.tmp-dist` 通过（直接 node vite.js；PS 5.1 下 2>&1 管道会中断 vite，重定向用 Start-Process 被禁，node 直跑最稳）；commit 15ee3b9 + tag v1.0.62 推送 dev 成功
- ⚠️ 新坑：cmd/git.exe 与 mingw64/bin/git.exe 均报 `remote-https is not a git command`（PortableGit 解包目录 mingw64/libexec/git-core 缺失）→ 解决：`git --exec-path="<PortableGit>/mingw64/bin"`（git-remote-https.exe 在 bin 下）

## v1.0.63 添加/编辑员工弹窗删除「所属小区」字段（dev）
- `src/views/staff/lists/index.vue`：删除弹窗「所属小区」下拉项；「负责楼栋」从 el-select（依赖所属小区联动过滤）改为 el-cascader 多选（小区 → 楼栋两级分组，值路径 [小区名, 楼栋名]，collapse-tags），不再依赖所属小区字段
- community 保存逻辑：楼栋管理员取第一个所选楼栋的小区名；保安/保洁沿用编辑原值、新增兜底 communityOptions[0]；communityIdMap/buildingOptions/onCommunityChange 已移除
- 楼栋唯一校验升级为逐个「小区+楼栋」路径比对，冲突提示含具体小区与楼栋（如「XX小区 2栋 已被管理员『张三』负责」）
- 编辑回填：row.buildings.split('、') 与 row.community 组合为路径数组；构建通过（47s，首次跑挂一次 exit 1 为瞬时故障，重跑即过）；commit d2bf57a + tag v1.0.63 已推送 dev
