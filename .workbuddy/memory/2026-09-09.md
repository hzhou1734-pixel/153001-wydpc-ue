# 2026-09-09 工作日志

## v1.0.7: 小区列表改为表格格式，按需求文档补齐字段（b355b6f，已推送）

用户指令：小区列表由卡片网格改为列表格式，并按需求文档的列表字段补齐。

### 变更明细
1. **src/views/community/lists/index.vue 全文重写（卡片 → 表格）**
   - 新增搜索区：小区名称关键词 + 位置 两个输入框，查询/重置按钮，与 usePaging `params: searchParams` 联动
   - el-table 12 列（对照需求文档）：小区门头（el-image 64×44 可预览）、小区名称（加粗）、位置、楼栋总数、总户数、已认证户数、员工总数、托管单总数、膳食单总数、陪诊单总数、状态（el-tag）、操作（详情/编辑/停用|启用/删除，fixed right）
   - 编辑弹窗（560px）补齐：小区门头（随机 picsum 封面 + 更换按钮）、省市区（el-cascader，页面内置 regionOptions：浙江/江苏两省下级市区数据，仅新增必填选满 3 级）、详细位置、楼栋数、房屋数、状态开关
2. **src/mock/data.ts**：communityList 5 条各补 4 个统计字段 staff_count / tuoguan_orders / shanshi_orders / peizhen_orders
3. **src/mock/api.ts**：getCommunityList 支持本地过滤（keyword 匹配名称、address 匹配位置）
4. **.gitignore 修复**：第 36 行 `.env.production.tmp-req.txt` 是坏行（两个规则挤一行），拆开并单独加 `.tmp-req.txt`；该文件曾被误提交，本次已 `git rm --cached` 并 amend 进 v1.0.7

### 流程记录
- `npx vite build --outDir=.tmp-dist` 构建成功（1m6s）→ `git clean -fd .tmp-dist`
- commit `v1.0.7: 小区列表改为表格格式，按需求文档补齐字段` + tag v1.0.7
- 首次 commit 后发现 .tmp-req.txt 误入（gitignore 坏行导致），修复后 amend（b355b6f），推送一次成功

### 后续待办（v1.0.9+ 差距清单）
- 订单三页补支付方式/支付时间、用户列表补账单总额/状态列、用户详情 7 tab、小区详情 7 板块、活动报名列表+停止报名、帮手审核流转、notice content 详情、wonderful/banner 新增
- 读取剩余 6 页（setting/{role,admin,property}、dashboard/{business,overview,todo}）完成全量核对

## v1.0.8: 员工管理楼栋管理员绑定楼栋选项修复

用户指令：员工列表，楼栋管理员缺少绑定楼栋的选项。

### 根因（3 项）
1. mock buildingTree 只有颐景园 3 栋，其他 4 个小区（绿城/保利/万科/融创）无楼栋数据 → 楼栋管理员若属于其他社区，下拉为空
2. 原 buildOptions 递归把单元节点混入楼栋下拉
3. 楼栋选项不与所属社区联动

### 变更明细
1. **src/mock/data.ts**：buildingTree 扩至 5 小区 10 栋，每楼栋补 community_name。颐景园(1-3栋)、绿城(11/12)、保利(21-23，覆盖孙丽华"3栋")、万科(35/36，覆盖马长顺"5栋、6栋"与刘洋"5栋1单元1103")、融创(41/42)；单元 id 编码「楼栋id×100+序号」式（如 2301）
2. **src/views/staff/lists/index.vue 7 处 MultiEdit**：
   - 新增 communityIdMap（社区名→小区ID）
   - onMounted 删除 buildOptions 调用
   - 删除 buildOptions 递归 → computed buildingOptions（filter community_id 后 map，仅楼栋层级）
   - 新增 onCommunityChange()：切社区清空 editForm.buildings
   - submitEdit 新增 buildingStr：仅 role_id===4 保存 join('、')，其他角色强制 '-'
   - 新增/编辑分支 buildings 字段改用 buildingStr
   - 模板所属社区 el-select 加 @change="onCommunityChange"

### 流程记录
- `npx vite build --outDir=.tmp-dist` 一次通过（1m2s，仅 chunk>500kB warning）→ `git clean -fd .tmp-dist`
- commit `v1.0.8: 员工管理楼栋管理员绑定楼栋选项修复` + tag v1.0.8 + push main --tags

## v1.0.9 员工管理头像编辑（e473cf5，已推送）
- 需求：员工信息缺少头像编辑选项
- 修复 3 处：① 新增/编辑弹窗顶部新增「员工头像」表单项（80px 预览 + 上传头像/随机生成/清除按钮）；② 新增分支改用 editForm.avatar（空则 picsum 兜底）；③ 编辑分支补 row.avatar 保存
- 实现方案：纯前端 FileReader + canvas 居中裁剪 200×200 jpeg(0.85) base64（mock 项目无上传接口，未用 material/upload 组件）；上传限 5MB、jpg/png/webp
- 构建 npx vite build --outDir=.tmp-dist 通过（51.6s，仅 chunk warning），git clean 清理后提交
- 教训：mock 驱动项目选纯前端方案，避免接不存在的后端接口

## v1.0.10 服务管理三页列表化 + 膳食去餐次（60713e9，已推送）

用户指令：服务管理的3个类型服务，全部修改为列表字段显示。并且膳食管理中不需要早餐中餐晚餐的说法。

### 变更明细
1. **src/views/service/meal/index.vue（6 处 MultiEdit）**
   - 删除页头 radio-group 餐次筛选（早餐/午餐/晚餐），仅保留标题「送餐服务」
   - 卡片区 → el-table：封面（el-image 预览）/ 餐品名称 / 价格（元，orange-500 加粗）/ 规格 / 供应商 / 排序 / 状态（el-switch）/ 操作（编辑+删除，fixed right）
   - 删除弹窗「餐品分类」form-item；usePaging 去掉 params.category 与 queryCategory ref；删除 categoryTag 函数；editForm 与 openAdd 去掉 category 字段
2. **src/views/service/nursing/index.vue**：卡片区 → el-table：封面 / 服务名称 / 服务类型（el-tag，保留 typeTag）/ 价格（¥+unit）/ 服务时长 / 服务描述 / 排序 / 状态 / 操作（fixed right）
3. **src/views/service/escort/index.vue**：卡片区 → el-table：封面 / 服务名称 / 价格（元/小时）/ 服务描述 / 排序 / 状态 / 操作（fixed right）

### 流程记录
- `npx vite build --outDir=.tmp-dist` 一次通过（41.58s，仅 chunk>500kB warning）→ `git clean -fd .tmp-dist`
- commit `v1.0.10: 服务管理三页改为列表格式，膳食管理去除早餐午餐晚餐分类` + tag v1.0.10 + push main --tags 一次成功
- 教训：Edit old_string 必须逐字精确（escort 页首次因拼错按钮文案失败，重读原文修正后成功）

## v1.0.11（2026-09-09 20:55）人力资源页面补充新增入口
- src/views/article/hr/index.vue：页头搜索框右侧新增「新增」按钮
- 弹窗改为新增/编辑共用（标题随 id 动态显示「新增/编辑招聘求职信息」）
- openAdd：重置表单；submitEdit：无 id 时 unshift 新记录（默认上架、发布时间取当前时间、id 用 Date.now()）
- 构建校验通过，已推送（commit aa3ab2c，tag v1.0.11）

## v1.0.12（2026-09-09 20:52）贴吧管理补充帖子正文与审核操作（7eee5ab，已推送）

用户指令：贴吧管理，缺少帖子的详情文本内容。缺少审核操作页面。

### 变更明细
1. **src/mock/data.ts**：barList 补 content（帖子正文）/audit（0 待审核 1 已通过 2 已拒绝）/audit_reason/audit_time 字段，新增 id=4、5 两条待审核帖子（现共 5 条）
2. **src/views/article/bar/index.vue 完全重写**：
   - 筛选 tab：el-radio-group queryAudit（全部/待审核/已通过/已拒绝），待审核带 el-badge 角标 pendingCount
   - 表格新增：审核状态列（el-tag，拒绝行 tooltip 显示原因）、上架状态列改仅 audit===1 显示 el-switch、操作列加「审核」按钮（仅待审核行）
   - 详情弹窗升级（560px）：「帖子内容」区块（左边框标题+灰底正文）、「审核信息」区块（el-descriptions：审核结果/审核时间/拒绝原因）、footer 含审核操作按钮（待审核→通过/拒绝，已审→重新审核）
   - 审核弹窗（480px）：拒绝必填原因 textarea（maxlength=100），通过时 el-alert 提示；submitAudit 更新 audit/audit_time/audit_reason，通过时自动上架 status=1
   - 辅助：reAudit 重新审核入口、nowTimeStr() 生成时间串；doAudit 冗余分支精简
3. 审核字段约定：audit 0 待审核 / 1 已通过 / 2 已拒绝，配 audit_reason、audit_time

### 流程记录
- `npx vite build --outDir=.tmp-dist` 一次通过（45.70s，仅 chunk warning）→ `git clean -fd .tmp-dist`
- commit 7eee5ab `v1.0.12: 贴吧管理补充帖子正文详情与审核操作功能` + tag v1.0.12，3 files changed, 188 insertions(+), 25 deletions(-)
- 推送一次成功（aa3ab2c..7eee5ab main → main，new tag v1.0.12）

## v1.0.13 精彩内容补充富文本详情与添加操作（2026-09-09）
- src/mock/data.ts：wonderfulList 3 条数据补 content 富文本 HTML（h4/p/img/ul/blockquote，图片 picsum seed ghj-wonderful-Xa/600/340）
- src/views/article/wonderful/index.vue 重写：
  - header 加「添加文章」按钮；操作列改 详情/编辑/删除 3 按钮（width 170）
  - 详情弹窗 640px：保留 descriptions + 新增「文章内容」区块 v-html 渲染，样式复用 bar 范式（.rich-content 深度选择器处理 img/h4/p/ul/blockquote）
  - 新增添加/编辑弹窗 680px：标题（必填，50字）、发布人（必填）、封面 input+预览（留空默认图）、状态 radio、富文本编辑器 wangEditor height 320（必填）
  - saveForm：编辑 find 更新；添加 unshift（id=Date.now(), views/likes=0, create_time 当前时间）
- 构建 40s 通过 → commit 5e6182a + tag v1.0.13 → push 成功

## v1.0.14 通知公告补充富文本详情与添加操作（2026-09-09）

用户指令：通知公告，缺少公告的富文本详情内容；缺少添加操作。

### 变更明细
1. **src/mock/data.ts**：noticeList 3 条全部补 content 富文本 HTML：
   - id1 停水检修（物业通知）：h4 停水检修安排 + ul 停水范围/恢复时间/施工影响 + 温馨提示 + blockquote 物业热线
   - id2 中秋游园（活动通知）：活动时间/地点/报名方式/截止 + 活动亮点 + img picsum seed ghj-notice-2a/600/340
   - id3 台风预警（安全提示）：台风动态 + 防范措施 + 物业值守 + 应急值班电话 blockquote
2. **src/views/article/notice/index.vue 全量重写（231 行）**：
   - header：card-title + 搜索框 + 「添加公告」按钮
   - 表格：标题列 el-link 可点开详情（+置顶 tag）、类型 tag、浏览量 sortable、状态 switch、发布时间；操作列详情/编辑/删除（width 170）
   - 详情弹窗 640px：el-descriptions（类型/置顶/浏览量/发布时间）+「公告内容」区块 v-html rich-content（样式复用 v1.0.13 范式）
   - 添加/编辑弹窗 680px destroy-on-close：标题（必填 maxlength50）、类型 radio-group（物业通知/活动通知/安全提示）、置顶 switch、公告内容 editor wangEditor height320（必填）
   - saveEdit：编辑 find 更新 / 添加 unshift（id=Date.now(), views=0, status=1, create_time=formatNow()）+ pager.total+=1
3. **乱码教训**：Write 写入后第 19 行 `label="类型"` 出现乱码，Edit 修复；全项目 grep 替换字符已清零。写入含中文文件后必须 grep `\xEF\xBF\xBD` 自检

### 流程记录
- `npx vite build --outDir=.tmp-dist` 通过（38.82s，仅 chunk warning）→ `git clean -fd .tmp-dist`
- commit `v1.0.14: 通知公告补充富文本详情与添加操作` + tag v1.0.14 → push main --tags

### 后续差距清单（v1.0.15+）
- banner 新增入口、活动报名列表+停止报名、帮手审核流转
- 订单三页补支付方式/支付时间、用户列表补账单总额/状态列、用户详情 7 tab、小区详情 7 板块
- 读取剩余 6 页（setting/{role,admin,property}、dashboard/{business,overview,todo}）完成全量核对
