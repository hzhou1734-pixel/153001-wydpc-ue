# 2026-09-09 工作日志

## v1.0.7: 小区列表改为表格格式，按需求文档补齐字段（b355b6f，已推送）

用户指令：小区列表由卡片网格改为列表格式，并按需求文档的列表字段补齐。

### 变更明细
1. **src/views/community/lists/index.vue 全文重写（卡片 → 表格）**
   - 新增搜索区：小区名称关键词 + 位置 两个输入框，查询/重置按钮，与 usePaging `params: searchParams` 联动
   - el-table 12 列（对照需求文档）：小区门头（el-image 64×44 可预览）、小区名称（加粗）、位置、楼栋总数、总户数、已认证户数、员工总数、托管单总数、膳食单总数、陪诊单总数、状态（el-tag）、操作（详情/编辑/停用|启用/删除，fixed right）
   - 编辑弹窗（560px）补齐：小区门头（随机 picsum 封面 + 更换按钮）、省市区（el-cascader，页面内置 regionOptions：浙江/江苏两省下级市区数据，仅新增必填选满 3 级）、详细位置、楼栋数、房屋数、状态开关
2. **src/mock/data.ts**：communityList 5 条各补 4 个统计字段 staff_count / tuoguan_orders / shanshi_orders / peizhen_orders
3. **src/mock/api.ts**：getCommunityList 支持本地过滤（keyword 匹配名称、address 匹配位置）
4. **.gitignore 修复**：第 36 行 `.env.production.tmp-req.txt` 是坏行（两个规则挤一行），拆开并单独加 `.tmp-req.txt`；该文件曾被误提交，本次已 `git rm --cached` 并 amend 进 v1.0.7

### 流程记录
- `npx vite build --outDir=.tmp-dist` 构建成功（1m6s）→ `git clean -fd .tmp-dist`
- commit `v1.0.7: 小区列表改为表格格式，按需求文档补齐字段` + tag v1.0.7
- 首次 commit 后发现 .tmp-req.txt 误入（gitignore 坏行导致），修复后 amend（b355b6f），推送一次成功

### 后续待办（v1.0.9+ 差距清单）
- 订单三页补支付方式/支付时间、用户列表补账单总额/状态列、用户详情 7 tab、小区详情 7 板块、活动报名列表+停止报名、帮手审核流转、notice content 详情、wonderful/banner 新增
- 读取剩余 6 页（setting/{role,admin,property}、dashboard/{business,overview,todo}）完成全量核对

## v1.0.8: 员工管理楼栋管理员绑定楼栋选项修复

用户指令：员工列表，楼栋管理员缺少绑定楼栋的选项。

### 根因（3 项）
1. mock buildingTree 只有颐景园 3 栋，其他 4 个小区（绿城/保利/万科/融创）无楼栋数据 → 楼栋管理员若属于其他社区，下拉为空
2. 原 buildOptions 递归把单元节点混入楼栋下拉
3. 楼栋选项不与所属社区联动

### 变更明细
1. **src/mock/data.ts**：buildingTree 扩至 5 小区 10 栋，每楼栋补 community_name。颐景园(1-3栋)、绿城(11/12)、保利(21-23，覆盖孙丽华"3栋")、万科(35/36，覆盖马长顺"5栋、6栋"与刘洋"5栋1单元1103")、融创(41/42)；单元 id 编码「楼栋id×100+序号」式（如 2301）
2. **src/views/staff/lists/index.vue 7 处 MultiEdit**：
   - 新增 communityIdMap（社区名→小区ID）
   - onMounted 删除 buildOptions 调用
   - 删除 buildOptions 递归 → computed buildingOptions（filter community_id 后 map，仅楼栋层级）
   - 新增 onCommunityChange()：切社区清空 editForm.buildings
   - submitEdit 新增 buildingStr：仅 role_id===4 保存 join('、')，其他角色强制 '-'
   - 新增/编辑分支 buildings 字段改用 buildingStr
   - 模板所属社区 el-select 加 @change="onCommunityChange"

### 流程记录
- `npx vite build --outDir=.tmp-dist` 一次通过（1m2s，仅 chunk>500kB warning）→ `git clean -fd .tmp-dist`
- commit `v1.0.8: 员工管理楼栋管理员绑定楼栋选项修复` + tag v1.0.8 + push main --tags

## v1.0.9 员工管理头像编辑（e473cf5，已推送）
- 需求：员工信息缺少头像编辑选项
- 修复 3 处：① 新增/编辑弹窗顶部新增「员工头像」表单项（80px 预览 + 上传头像/随机生成/清除按钮）；② 新增分支改用 editForm.avatar（空则 picsum 兜底）；③ 编辑分支补 row.avatar 保存
- 实现方案：纯前端 FileReader + canvas 居中裁剪 200×200 jpeg(0.85) base64（mock 项目无上传接口，未用 material/upload 组件）；上传限 5MB、jpg/png/webp
- 构建 npx vite build --outDir=.tmp-dist 通过（51.6s，仅 chunk warning），git clean 清理后提交
- 教训：mock 驱动项目选纯前端方案，避免接不存在的后端接口

## v1.0.10 服务管理三页列表化 + 膳食去餐次（60713e9，已推送）

用户指令：服务管理的3个类型服务，全部修改为列表字段显示。并且膳食管理中不需要早餐中餐晚餐的说法。

### 变更明细
1. **src/views/service/meal/index.vue（6 处 MultiEdit）**
   - 删除页头 radio-group 餐次筛选（早餐/午餐/晚餐），仅保留标题「送餐服务」
   - 卡片区 → el-table：封面（el-image 预览）/ 餐品名称 / 价格（元，orange-500 加粗）/ 规格 / 供应商 / 排序 / 状态（el-switch）/ 操作（编辑+删除，fixed right）
   - 删除弹窗「餐品分类」form-item；usePaging 去掉 params.category 与 queryCategory ref；删除 categoryTag 函数；editForm 与 openAdd 去掉 category 字段
2. **src/views/service/nursing/index.vue**：卡片区 → el-table：封面 / 服务名称 / 服务类型（el-tag，保留 typeTag）/ 价格（¥+unit）/ 服务时长 / 服务描述 / 排序 / 状态 / 操作（fixed right）
3. **src/views/service/escort/index.vue**：卡片区 → el-table：封面 / 服务名称 / 价格（元/小时）/ 服务描述 / 排序 / 状态 / 操作（fixed right）

### 流程记录
- `npx vite build --outDir=.tmp-dist` 一次通过（41.58s，仅 chunk>500kB warning）→ `git clean -fd .tmp-dist`
- commit `v1.0.10: 服务管理三页改为列表格式，膳食管理去除早餐午餐晚餐分类` + tag v1.0.10 + push main --tags 一次成功
- 教训：Edit old_string 必须逐字精确（escort 页首次因拼错按钮文案失败，重读原文修正后成功）

## v1.0.11（2026-09-09 20:55）人力资源页面补充新增入口
- src/views/article/hr/index.vue：页头搜索框右侧新增「新增」按钮
- 弹窗改为新增/编辑共用（标题随 id 动态显示「新增/编辑招聘求职信息」）
- openAdd：重置表单；submitEdit：无 id 时 unshift 新记录（默认上架、发布时间取当前时间、id 用 Date.now()）
- 构建校验通过，已推送（commit aa3ab2c，tag v1.0.11）

## v1.0.12（2026-09-09 20:52）贴吧管理补充帖子正文与审核操作（7eee5ab，已推送）

用户指令：贴吧管理，缺少帖子的详情文本内容。缺少审核操作页面。

### 变更明细
1. **src/mock/data.ts**：barList 补 content（帖子正文）/audit（0 待审核 1 已通过 2 已拒绝）/audit_reason/audit_time 字段，新增 id=4、5 两条待审核帖子（现共 5 条）
2. **src/views/article/bar/index.vue 完全重写**：
   - 筛选 tab：el-radio-group queryAudit（全部/待审核/已通过/已拒绝），待审核带 el-badge 角标 pendingCount
   - 表格新增：审核状态列（el-tag，拒绝行 tooltip 显示原因）、上架状态列改仅 audit===1 显示 el-switch、操作列加「审核」按钮（仅待审核行）
   - 详情弹窗升级（560px）：「帖子内容」区块（左边框标题+灰底正文）、「审核信息」区块（el-descriptions：审核结果/审核时间/拒绝原因）、footer 含审核操作按钮（待审核→通过/拒绝，已审→重新审核）
   - 审核弹窗（480px）：拒绝必填原因 textarea（maxlength=100），通过时 el-alert 提示；submitAudit 更新 audit/audit_time/audit_reason，通过时自动上架 status=1
   - 辅助：reAudit 重新审核入口、nowTimeStr() 生成时间串；doAudit 冗余分支精简
3. 审核字段约定：audit 0 待审核 / 1 已通过 / 2 已拒绝，配 audit_reason、audit_time

### 流程记录
- `npx vite build --outDir=.tmp-dist` 一次通过（45.70s，仅 chunk warning）→ `git clean -fd .tmp-dist`
- commit 7eee5ab `v1.0.12: 贴吧管理补充帖子正文详情与审核操作功能` + tag v1.0.12，3 files changed, 188 insertions(+), 25 deletions(-)
- 推送一次成功（aa3ab2c..7eee5ab main → main，new tag v1.0.12）

## v1.0.13 精彩内容补充富文本详情与添加操作（2026-09-09）
- src/mock/data.ts：wonderfulList 3 条数据补 content 富文本 HTML（h4/p/img/ul/blockquote，图片 picsum seed ghj-wonderful-Xa/600/340）
- src/views/article/wonderful/index.vue 重写：
  - header 加「添加文章」按钮；操作列改 详情/编辑/删除 3 按钮（width 170）
  - 详情弹窗 640px：保留 descriptions + 新增「文章内容」区块 v-html 渲染，样式复用 bar 范式（.rich-content 深度选择器处理 img/h4/p/ul/blockquote）
  - 新增添加/编辑弹窗 680px：标题（必填，50字）、发布人（必填）、封面 input+预览（留空默认图）、状态 radio、富文本编辑器 wangEditor height 320（必填）
  - saveForm：编辑 find 更新；添加 unshift（id=Date.now(), views/likes=0, create_time 当前时间）
- 构建 40s 通过 → commit 5e6182a + tag v1.0.13 → push 成功

## v1.0.14 通知公告补充富文本详情与添加操作（2026-09-09）

用户指令：通知公告，缺少公告的富文本详情内容；缺少添加操作。

### 变更明细
1. **src/mock/data.ts**：noticeList 3 条全部补 content 富文本 HTML：
   - id1 停水检修（物业通知）：h4 停水检修安排 + ul 停水范围/恢复时间/施工影响 + 温馨提示 + blockquote 物业热线
   - id2 中秋游园（活动通知）：活动时间/地点/报名方式/截止 + 活动亮点 + img picsum seed ghj-notice-2a/600/340
   - id3 台风预警（安全提示）：台风动态 + 防范措施 + 物业值守 + 应急值班电话 blockquote
2. **src/views/article/notice/index.vue 全量重写（231 行）**：
   - header：card-title + 搜索框 + 「添加公告」按钮
   - 表格：标题列 el-link 可点开详情（+置顶 tag）、类型 tag、浏览量 sortable、状态 switch、发布时间；操作列详情/编辑/删除（width 170）
   - 详情弹窗 640px：el-descriptions（类型/置顶/浏览量/发布时间）+「公告内容」区块 v-html rich-content（样式复用 v1.0.13 范式）
   - 添加/编辑弹窗 680px destroy-on-close：标题（必填 maxlength50）、类型 radio-group（物业通知/活动通知/安全提示）、置顶 switch、公告内容 editor wangEditor height320（必填）
   - saveEdit：编辑 find 更新 / 添加 unshift（id=Date.now(), views=0, status=1, create_time=formatNow()）+ pager.total+=1
3. **乱码教训**：Write 写入后第 19 行 `label="类型"` 出现乱码，Edit 修复；全项目 grep 替换字符已清零。写入含中文文件后必须 grep `\xEF\xBF\xBD` 自检

### 流程记录
- `npx vite build --outDir=.tmp-dist` 通过（38.82s，仅 chunk warning）→ `git clean -fd .tmp-dist`
- commit `v1.0.14: 通知公告补充富文本详情与添加操作` + tag v1.0.14 → push main --tags

### 后续差距清单（v1.0.15+）
- banner 新增入口、活动报名列表+停止报名、帮手审核流转
- 订单三页补支付方式/支付时间、用户列表补账单总额/状态列、用户详情 7 tab、小区详情 7 板块
- 读取剩余 6 页（setting/{role,admin,property}、dashboard/{business,overview,todo}）完成全量核对

## v1.0.15: 社区活动补充富文本详情与添加操作

用户指令：社区活动，缺少富文本详情内容；缺少添加操作。

### 变更明细
1. **src/mock/data.ts**：activityList 3 条全部补 content 富文本 HTML：
   - id1 中秋游园会（进行中 186/300）：活动介绍 + 活动安排（时间/地点/报名方式/截止）+ 活动亮点（猜灯谜/亲子灯笼DIY/茶话会/抽奖）+ 温馨提示 + img picsum ghj-activity-1a + 咨询热线 blockquote
   - id2 老年健康义诊（进行中 92/150）：活动背景（联合市第三人民医院）+ 义诊项目（基础体检/内科/骨科/中医理疗）+ 参与须知 + 咨询电话 blockquote
   - id3 周末亲子手工课第12期（已满员 40/40）：课程介绍（非遗竹编小船）+ 课程安排 + 课堂流程 + 温馨提示 + img ghj-activity-3a + 咨询电话 blockquote
2. **src/views/article/activity/index.vue 全量重写（约 290 行）**：
   - header：card-title + 搜索框 + 「添加活动」按钮
   - 表格：标题列 el-link 可点开详情（signup>=limit 显示「已满员」tag）、报名进度 el-progress、开始时间、活动地点、状态 switch、创建时间；操作列详情/编辑/删除（width 170 fixed right）
   - 详情弹窗 640px top 6vh：el-descriptions（标题/开始时间/状态/报名人数/创建时间/地点/封面）+「活动详情」区块 v-html rich-content + max-height 420px overflow-y auto 滚动
   - 添加/编辑弹窗 680px destroy-on-close：活动标题（必填 maxlength50）、活动地点（必填 maxlength50）、开始时间 el-date-picker datetime（必填）、报名上限 el-input-number 1-9999（必填）、活动封面 el-input 可留空自动生成 picsum、上架状态 switch、活动详情 editor wangEditor height320（必填）
   - saveEdit：编辑 find 更新（cover 留空保留原值）/ 添加 unshift（id=Date.now(), views=0, signup=0, cover 自动生成, create_time=formatNow()）+ pager.total+=1
3. **乱码自检**：Write 后 grep `\xEF\xBF\xBD` 通过（No matches found），一次通过无乱码

### 流程记录
- `npx vite build --outDir=.tmp-dist` 通过（42.25s，仅 chunk >500kB warning）→ `git clean -fd .tmp-dist`
- commit `v1.0.15: 社区活动补充富文本详情与添加操作` + tag v1.0.15 → push origin main --tags

### 后续差距清单（v1.0.16+）
- banner 新增入口、活动报名列表+停止报名、帮手审核流转
- 订单三页补支付方式/支付时间、用户列表补账单总额/状态列、用户详情 7 tab、小区详情 7 板块
- 读取剩余 6 页（setting/{role,admin,property}、dashboard/{business,overview,todo}）完成全量核对

## v1.0.16: 生活帮手改为用户提交表单查看（d0ff6a8，已推送）

用户指令：生活帮手，是用户提交的生活帮助表单。来看用户提交的表单信息的，而不是平台发布开锁之类的服务。对照需求文档查看。

### 变更要点
- src/views/article/helper/index.vue 全量重写：由「平台发布服务」改为「用户提交的帮助请求列表」
- 表格字段：申请人（头像+姓名+电话）/ 帮助类型（el-tag）/ 服务地址 / 期望上门时间 / 问题描述 / 状态（待接单/处理中/已完成/已取消）/ 提交时间；操作列 查看/删除
- 详情弹窗：el-descriptions 完整展示申请人信息、帮助类型、地址、期望时间、问题描述、状态、处理备注
- mock helperList 调整为用户提交表单数据（张阿姨水管维修、李叔家电清洗、王女士陪同就医等）

## v1.0.17: banner新增添加入口并优化跳转内容设置

用户指令：banner图，缺少添加入口，需要优化跳转内容设置。

### 变更明细
1. **src/views/article/banner/index.vue 全量重写（242 行）**
   - 卡片头：标题 + 搜索框 + 「新增轮播图」按钮（type=primary + Plus 图标 @click=openAdd）
   - 表格 8 列：图片（el-image 预览）/ 名称 / **跳转类型（el-tag 动态：活动详情 warning、膳食服务/陪诊服务 success、内置页面、自定义链接、不跳转 info）** / 跳转目标 / 排序 sortable / 状态 el-switch / 创建时间 / 操作（编辑/删除 fixed right width 140）
   - 新增/编辑共用弹窗（560px）：名称必填、图片地址必填+预览、**跳转类型 select（required，@change=onTypeChange 切换清空）**，四类联动表单：
     - activity：关联活动下拉（activityOptions 来自 getActivityList，label `#id 标题`），提示自动生成 `/pages/activity/detail?id=活动ID`
     - page：builtinPages 下拉（膳食/陪诊/家政/帮手/精彩内容列表/首页 tabbar 共 6 项）
     - custom：自定义路径 el-input
     - none：无附加表单
   - 排序 el-input-number 0-999（新增默认 = 现有条数+1）
2. **方案**：按 link 前缀推断类型（`/pages/activity/detail` → activity；builtinPages 全匹配 → page；空 → none；其余 → custom），openEdit 正则 `/[?&]id=(\d+)/` 回填活动 id，无需修改 mock 字段
3. **saveEdit**：编辑 find 原地赋值；新增 maxId+1 + unshift（status=1，create_time 本地时间）+ pager.total+=1
4. **乱码/路径教训**：Bash 中含中文完整路径会被传输损坏（「端」字变乱码报 No such file），改用 `cd 项目目录 && 相对路径` 形式；Read 输出验证无 U+FFFD

### 流程记录
- `npx vite build --outDir=.tmp-dist` 构建通过（1m9s，仅 chunk >500kB warning）→ `git clean -fd .tmp-dist`
- commit `v1.0.17: banner新增添加入口并优化跳转内容设置` + tag v1.0.17 → push origin main --tags

### 后续差距清单（v1.0.18+）
- 活动报名列表 + 停止报名
- 订单三页补支付方式/支付时间字段
- 用户列表补账单总额/状态列、用户详情 7 tab
- 小区详情 7 板块
- 剩余 6 页全量核对（setting/{role,admin,property}、dashboard/{business,overview,todo}）

## v1.0.18: 社区活动报名管理 + 订单支付字段 + .env.production 纳管（8196ff5，已强推）

### 本版本代码成果
1. **社区活动（article/activity/index.vue +75 行）**：操作列新增「报名」（查看报名列表弹窗 820px：头像昵称/电话/房号/人数/时间/备注+搜索）与「停止报名」按钮（signup_status===1 时显示）；详情弹窗补报名状态 tag；分页绑定修正 pager.limit/total → pager.size/count；mock/api.ts 新增 getActivitySignupList（activity_id/keyword 过滤）+ data.ts activitySignupList 数据
2. **订单三页（order/{escort,meal,nursing}/index.vue 各+8 行）**：表格支付状态列后插入「支付方式」（row.pay_type || '—'）与「支付时间」列；详情弹窗补对应 descriptions-item

### 用户强推指令执行记录
- 用户指令：「.env.production 把这个文件夹强推到GitHub的main分支」
- 执行：① .gitignore 删除 `.env.production` 排除行（保留 .env.development）② git add -A（8 文件）③ commit `v1.0.18: 社区活动报名管理与订单支付字段完善，.env.production纳入版本管理` ④ tag v1.0.18 ⑤ `push --force origin main --tags`（TOKEN 鉴权 extraHeader 方式）一次成功
- push 结果：c9d0f1a..8196ff5 main -> main，新 tag v1.0.18；fetch 后远程跟踪恢复（main...origin/main 同步，此前 [origin/main: gone] 已修复）

### 剩余差距清单（v1.0.19+）
- 用户管理：列表补账单总额/状态列 + 用户详情多 tab（Task #8，in_progress）
- 小区管理：小区详情 7 板块（Task #10）
- 全量核对剩余 6 页 + 构建 + 提交（Task #11）

## v1.0.19: 小区统计字段改为系统自动生成（4530377）
- 小区编辑弹窗中的「楼栋数」「房屋数」改为只读统计展示，明确由「楼栋房屋管理」数据自动统计。
- 保存编辑时不再回写 buildings / houses，新增小区仍以 0 为初始值，后续随楼栋与房屋数据维护更新。
- 已完成构建和本地 commit/tag；GitHub 主站 443 持续不可达，推送待网络恢复后补发。

## v1.0.20: 用户管理列表与详情信息补齐
- `src/mock/api.ts`：用户列表增加关键词、小区、账户状态筛选，并提供按用户 ID 获取详情的 mock 接口。
- `src/views/consumer/lists/index.vue`：列表补齐所属小区、房屋信息、业主认证、账户余额、服务订单、账户状态及对应筛选条件。
- `src/views/consumer/lists/detail.vue`：详情改为基本信息、资金流水、服务订单 3 个标签页；资金和订单均按当前用户聚合 mock 数据，余额调整在前端 mock 内即时生效。
- 生产构建通过（仅保留既有 Vite 环境与大包体告警）。

## v1.0.21: 小区详情补齐七个业务板块
- `src/views/community/detail/index.vue` 扩展为基本信息、楼栋管理、房屋信息、认证住户、绑定员工、服务订单、通知公告 7 个标签页。
- 楼栋、房屋、认证、员工与订单均按当前小区实时聚合 mock 数据；楼栋数/房屋数维持为只读系统统计。
- 楼栋和住户认证板块提供前往对应维护页面的入口；生产构建通过。

## v1.0.22: 角色管理补新增/编辑/删除，管理员补新增/删除与超管保护（1fe51c4）
- permission/role 与 permission/admin 两页补齐增删改与超管不可删除保护逻辑，构建通过。

## v1.0.23: 系统内所有手机号显示完整号码，去除 **** 脱敏（b707607）
- 用户指令：系统内所有手机号码需完整显示，不允许 **** 替代。
- 全量 sed 替换 44 处：`****` → `1234` 统一填充（如 `138****1001` → `13812341001`），同一人跨多张表自动对齐同号（张伟 5 处）。
- 注意：`grep '\*\*\*\*' src/` 会误命中 `src/assets/images/red2.png`（二进制巧合匹配），非文本脱敏，无需处理。

## v1.0.24: 小区详情页楼栋房屋与认证住户直接展示具体数据（543a3b4，已推送）

用户指令：详情页面中，楼栋房屋和认证住户，需要把具体的数据体现出来，而不是给个提示词去其它页面查看。

### 变更明细
1. **src/views/community/detail/index.vue**
   - 删除 `goBuildingManagement` / `goCertification` 两个跳转函数及两处「前往xx管理」按钮（数据在本页直接呈现）
   - 楼栋管理页签改为 **el-table 展开行**：主表列 = 楼栋名称 / 单元数量 / 单元明细 / 登记房屋 / 已认证房屋 / 维护状态；展开后按单元分组，以 el-tag 列出「房号 · 业主 · 手机号」，已认证绿色、未认证灰色，无数据单元提示「该单元暂无登记房屋」
   - 硬编码「已维护」改为按实际房号数动态判断（有房号=已维护，无=待完善）
   - 认证住户页签新增「身份类型」列（业主），单元/房号分隔改为 `1单元/101`
   - 新增辅助函数：`unitRooms(buildingId, unitId)`、`buildingRoomCount`、`buildingCertifiedCount`、`buildingUnitNames`
2. **src/mock/data.ts 大幅扩充**
   - buildingTree：颐景园·江南里由 3 栋补足 **12 栋**（id 1/2/3/4~10/**13**/**14**；11栋、12栋刻意用 id 13/14 避开绿城已占用的 11/12，保证楼栋 id 与单元 id 全局唯一），单元 id 沿用「楼栋id×100+序号」
   - roomList：5 条 → **65 条**，覆盖 5 个小区全部楼栋单元，手机号延续 13812341001~10067 完整格式
   - certifyList：3 条 → **12 条**（3 条待审核：张伟/王强/赵敏；9 条已通过），与 roomList 的业主姓名、手机号、楼栋单元房号严格对齐
   - 数据一致性修正：consumerList 张伟/王强 `certified` 1→0（与 certifyList 待审核状态对齐）；dashboardTodo「待审核认证申请」count 1→3

### 数据一致性约定（后续扩充须遵守）
- roomList.certified === 1 ⟺ certifyList.status === 1（已通过）；certified 0 ⟺ status 0（待审核）或无记录
- 同一业主在 consumerList / roomList / certifyList 三张表中手机号必须完全一致

### 流程记录
- 构建：`rm -rf .tmp-dist && npx vite build --outDir=.tmp-dist` 通过（1m16s，仅 chunk warning）→ `git clean -fd .tmp-dist`
- **safe-delete 坑**：WorkBuddy 的 node-safe-delete-shim.cjs 会把 fs.rmSync 转 trashItem，导致 vite emptyDir 被中止；必须先 `rm -rf .tmp-dist` 再构建
- push 一次成功：cf700ef..543a3b4 main → main，新 tag v1.0.22 / v1.0.23 / v1.0.24

## v1.0.25: 楼栋房号列表编辑与删除功能修复（2c1d589，已推送）

### 用户指令
楼栋房号页面-房号列表的编辑，目前点击没有响应。

### 根因
`src/views/community/building/index.vue` 操作列「编辑」「删除」两个 el-button 均无 `@click` 绑定，script 中也没有对应函数，点击自然无响应。

### 修复内容（MultiEdit 3 处）
1. **script 追加约 90 行**：
   - `showEdit` / `editForm`（id/community_id/building_id/unit_id/name/owner/phone）
   - `openEdit(row)`：回填房号/业主/电话，**用 building_id 在 buildingTree 反查所属小区回填 community_id**；owner/phone 为 '-' 时清空为 ''
   - 级联联动复用新增弹窗模式：`editBuildingOptions`（按小区 filter）、`editUnitOptions`（按楼栋 find children）；小区 change 清空楼栋+单元，楼栋 change 清空单元
   - `submitEdit()`：find by id 后原地更新；owner/phone 空则存 '-'
   - `handleDelete(row)`：ElMessageBox.confirm 确认后 splice 删除
2. **操作列绑定事件**：`@click="openEdit(row)"` / `@click="handleDelete(row)"`
3. **追加「编辑房号」el-dialog**（460px），字段与新增弹窗一致：小区→楼栋→单元级联 + 房号必填 + 业主/电话选填

### 流程记录
- 构建：`rm -rf .tmp-dist && npx vite build --outDir=.tmp-dist` 通过（1m27s）→ `git clean -fd .tmp-dist`
- commit `2c1d589 v1.0.25: 楼栋房号列表编辑与删除功能修复` + tag v1.0.25（3 files changed, 159 insertions, 2 deletions，含 memory 文件 CRLF warning）
- push 一次成功：543a3b4..2c1d589 main → main，新 tag v1.0.25
- 下一版本号：**v1.0.26**

## 16:40 员工列表布局优化（v1.0.26）
- 反馈：员工列表左侧空泛、右侧拥挤
- 调整：src/views/staff/lists/index.vue 列宽重分配（员工 180→160、社区 140→130、楼栋 120→150+tooltip、订单 90→110、收益 120→140、状态 90→100、入职时间 120→150、操作 140→160、角色 110→120），新增单元格内边距 padding 14px
- 构建通过，已 commit + tag v1.0.26 + push 成功（下一版本号 v1.0.27）

## 16:55 员工编辑弹窗优化（v1.0.27）
- 反馈：编辑员工信息中「随机生成」「清除」两个头像入口删除，补充员工账号状态操作
- 修改 src/views/staff/lists/index.vue（MultiEdit 8 处）：
  1. 删除 onAvatarRandom/onAvatarClear 两函数及弹窗对应按钮（仅留上传头像）
  2. editForm 加 status 字段；openAdd 重置 status:1；openEdit 回填 row.status
  3. 新增保存 status:1→editForm.status；编辑保存同步 row.status
  4. 弹窗「所属社区」后新增「账号状态」radio-group（:value 1启用/0停用）
- 构建通过（1m29s），commit 3ca010f + tag v1.0.27 + push 成功（下一版本号 v1.0.28）

## 17:10 用户列表数据展示与用户详情 7 板块（v1.0.28）
- 反馈：用户列表点击跳转登录页、无数据；需求 = 筛选搜索 + 字段信息 + 用户详情（基础/订单/结算/帖子/资源/手环/报名 7 板块）+ 启用禁用仅查看不可修改
- 变更文件（4 files changed, 377 insertions, 13 deletions）：
  1. src/mock/data.ts：activitySignupList 5 条改为 consumerList 用户（102 李娜/103 王强/104 赵敏/203 陈晨/301 刘洋）；文末 paginate 前新增 userSettleList（9条）/healthBandList（6条）/resourceList（5条）
  2. src/views/consumer/lists/index.vue：perms 修正为 `getRoutePath('consumer.lists/detail')`（原 user.user/detail 导致跳登录页）；新增 ID/未结算金额/已结算金额列，补回注册时间列
  3. src/views/consumer/detail/index.vue 新建（约 280 行）：顶部返回按钮+状态 tag，el-tabs 7 板块（base/order/settle/post/resource/band/signup）；订单/结算状态 map（0待接单/1服务中/2已完成/3已取消；0待确认/1结算中/2已结算）；账号状态仅展示不可修改
- 关键修正：detail 页 mock 导出名 orderNursingList/orderMealList/orderEscortList → nursingOrders/mealOrders/escortOrders（MultiEdit 4 处：import + 3 个 filter 引用）
- ⚠️ safe-delete shim 新坑：`rm -rf .tmp-dist` 有时被 shim 拦截 FAIL_CLOSED（genie-trash exit 1）。应对：`git clean -fd .tmp-dist` 后直接 `npx vite build --outDir=.tmp-dist`
- 构建通过（1m9s，仅 chunk >500kB warning）
- commit `16ddcf0 v1.0.28: 用户列表与用户详情页实现，修复点击跳转登录页问题` + tag v1.0.28 + push 一次成功（3ca010f..16ddcf0）
- 下一版本号：**v1.0.29**

## 17:20 线上部署白屏排查与修复（v1.0.29）
- 现象：用户用 EdgeOne 部署仓库到 `1530-01-wyd.admin.ue.ehaiba.net`，访问空白
- 排查（curl 忽略证书：`curl -sSk`；Git Bash 下 `/dev/null`、`/tmp` 写入会报 curl(23)，改用 `| head -c`）：
  1. 根路径返回 200 + 构建后 index.html（说明 EdgeOne 构建执行成功、部署的是 dist）
  2. HTML 内资源引用为 `/admin/assets/index-xxx.js`
  3. 请求 `/admin/assets/index-xxx.js` → 返回 200 但 Content-Type: text/html（内容正是 index.html）→ EdgeOne 未匹配路径回退 index.html，浏览器 module 脚本 MIME 不匹配被拒绝执行 → 白屏
- **根因**：`vite.config.ts` 的 `base: '/admin/'`（为 server/public/admin 部署而设），与域名根部署不匹配
- 修复：
  1. vite.config.ts → `base: process.env.VITE_PUBLIC_PATH || '/'`（可用 `--base=/admin/` 覆盖）
  2. package.json → `"build": "vite build"`（干净构建给 EdgeOne）；新增 `"build:release": "vite build --base=/admin/ && node scripts/release.mjs"`（保留原 server/public/admin 发布）
- 验证：构建产物 index.html 引用 `/assets/...`，`grep -c '/admin/assets'` = 0
- 其他确认项：路由 `createWebHistory(import.meta.env.BASE_URL)`；后端 `https://likeadmin.l.440.red:9443` 连通（HTTP 200）；登录走 mock，业务页 66 个引用 @/api、31 个引用 @/mock/api
- commit `e312dac v1.0.29` + tag v1.0.29 + push 成功（16ddcf0..e312dac）
- 下一版本号：**v1.0.30**

## v1.0.30 服务管理-托管服务删除服务类型
- 确认菜单映射：mock/user.ts 中「托管服务」菜单 -> service/nursing/index.vue（页面标题「养老服务」）
- 删除内容：列表「服务类型」列、编辑弹窗「服务类型」表单项、typeTag 函数、editForm.type 字段及 openAdd 默认值
- 构建验证通过，commit 21a507a + tag v1.0.30 已推送

## v1.0.31 膳食服务删除规格/供应商并新增菜品信息富文本
- 依据用户截图红框：删除列表「规格」「供应商」两列及新增/编辑弹窗中对应表单项
- 新增「菜品信息」富文本编辑器（复用 @/components/editor wangEditor 组件，simple 模式 260px 高），绑定 editForm.detail，用于编辑菜品图文信息
- 弹窗宽度 520px -> 760px、top=5vh 以容纳富文本；editForm 移除 spec/vendor 字段并新增 detail（openEdit 兜底 row.detail || ""）
- 构建验证通过，commit 8d9ee1a + tag v1.0.31 已推送

## v1.0.32 修复登录页广告图破裂
- 根因：src/mock/config.ts 的 mockConfig 缺少 login_image 字段，登录页 image-contain src 为 undefined 显示破裂占位
- 修复：补充 login_image = https://picsum.photos/seed/ghj-login-banner/800/800
- 构建验证通过，commit d8e6fa1 + tag v1.0.32 已推送

## v1.0.33 移除系统全部余额概念
- 用户列表：删除「账户余额」列；用户详情（两处 consumer/detail 与 consumer/lists/detail）删除「账户余额」展示项及「调整余额」按钮
- 删除文件：src/views/finance/balance_details.vue（余额明细页，无菜单引用）、src/views/consumer/components/account-adjust.vue（余额调整弹窗）
- 财务概况：删除「账户余额」统计卡片与资金说明项
- mock/data.ts：consumerList 6 条记录移除 balance 字段；financeOverview 移除 balance；4 处 pay_type 由「余额支付」改为「微信支付」
- api/finance.ts 移除 accountLog、getUmChangeType；setting/pay/config/edit.vue 移除 PayWayEnum.BALANCE 与「余额支付」分支
- 全局 grep「余额|balance」已无残留；构建通过，commit 71e21ec + tag v1.0.33 已推送

## v1.0.34 托管服务改造 + 陪诊单价
- 托管服务（service/nursing/index.vue 重写）：搜索区（服务标题关键字、托管类型、托管状态、添加时间区间）；列表字段：托管服务标题/开始时间/结束时间/托管类型/托管价格/报名人数/托管状态/添加时间
- 托管类型固定为：日托、学期每日托、学期周末托；托管状态：显示 / 隐藏（操作列提供显示隐藏切换，隐藏后用户端不提供报名）
- 添加/编辑弹窗：标题、类型、价格、开始时间、结束时间、排序、状态；校验结束时间不早于开始时间
- mock：nursingServices 重写为 8 条（含 start_time/end_time/signup_count/create_time），nursingOrders service 名称同步为新托管标题
- mock/api：getNursingServiceList 支持 keyword/type/status/start_time/end_time 过滤
- 陪诊服务页顶部新增「陪诊服务单价」卡片：hour_price（元/小时）、min_hours（最低计费时长）、remark，mock api getEscortPriceSetting/saveEscortPriceSetting

## v1.0.35 膳食服务改为套餐组合固定价
- 用户确认计价模式为「套餐组合固定价」（选 ABC 一个价、选 BCD 另一个价）
- 膳食页改为三个 Tab：菜品管理（图片/名称/价格/菜品信息富文本）、套餐组合（组合名称+多选菜品+组合价格+显示隐藏，列表显示菜品原价合计划线对比）、每日菜单（按日期勾选当日供应套餐并保存）
- mock 新增 mealCombos（6 条组合）、dailyMenus（按日期配置）、api getMealComboList/getDailyMenu/saveDailyMenu；mealOrders service 同步为组合名称

## v1.0.36 订单管理三大页面重构
- 托管订单：关键字（订单编号/托管名称/昵称/手机号）+ 订单状态/支付状态/支付方式/下单时间/支付时间筛选；字段：订单编号、托管名称、托管类型、价格、下单人头像昵称、手机号、所属楼栋、订单状态、支付状态、下单时间、支付时间、支付方式
- 托管派单：仅「需要接送」且「待派单」的订单可派单，选择 role_id=1 托管员；详情含托管/支付人/地址/金额/订单日志五段
- 膳食订单：关键字（订单编号/昵称/手机号）+ 是否需要配送/楼栋/支付状态/支付方式/下单时间/支付时间；字段含是否需要配送、楼栋；派单选 role_id=2 配送员（需配送且待派单才可派）
- 陪诊订单：关键字 + 订单状态/支付状态/支付方式/下单时间/支付时间；字段含金额；详情为陪诊/支付人/金额/订单日志四段（无地址）；派单选 role_id=3 陪诊员
- mock：三张订单表重写为 8 条/表（含 avatar/nickname/mobile/building/payer/address/logs 等），员工表扩充到 11 人（托管员/配送员/陪诊员各多名）
- mock/api：新增 filterOrders 通用筛选 + dispatchOrder 派单接口；同步修正 consumer/community 详情页对订单 user/service 字段的引用（user->nickname，膳食 service->combo）

## v1.0.37 内容管理七大模块重构
- 人力资源(hr)：关键词(标题/昵称/手机号)+发布状态/发布时间/审核时间筛选；字段=资源标题、发布人(头像+昵称，物业后台显示发布账号)、手机号、发布状态、发布时间、审核时间
  - 规则：source=1 物业后台发布(免审核、审核时间=发布时间、默认置顶、可编辑)；source=2 用户发布(需审核、不可编辑)
  - 操作：添加资源(标题/详情内容/状态)、编辑(仅物业后台)、审核(通过/违规驳回填原因)、显示隐藏、删除
- 社区贴吧(bar)：关键词(帖子ID/标题/昵称/手机号)+帖子状态/发布时间/审核时间；字段=帖子ID、封面图、标题、发布人头像昵称、手机号、帖子状态、发布时间、审核时间
  - 操作：详情(发布人/帖子/收藏评论数/发布日志时间轴)、审核(通过/违规重审下架填备注)、评论列表(新增 barComments mock)、显示隐藏、删除
- 精彩内容(wonderful)：文章ID/标题搜索 + 显示状态/添加时间；字段=文章ID、封面图、标题、显示状态、排序、添加时间；含详情/添加/编辑/删除/显示隐藏
- 社区通知(notice)：标题搜索 + 显示状态/添加时间；字段=通知标题、显示状态、排序、浏览量、添加时间(移除类型与置顶展示)
- 社区活动(activity)：标题搜索 + 活动状态(报名中/已结束)/发布时间；字段=封面图、标题、活动状态、活动时间、报名人数/总人数、排序、发布时间
  - 活动时间=文字输入；报名时间=日期范围(超过报名结束时间或手动停止报名即已结束)；新增报名列表(头像/昵称/手机号/报名时间)
- 生活帮手(helper)：关键词(昵称/手机号/处理人)+类型/楼栋/处理状态/提交时间/处理时间；处理人改为下拉选择物业后台管理员账号(adminList)
- Banner图：标题搜索+状态/添加时间；字段=标题、封面图、排序、状态、添加时间；跳转设置改为选填
- mock/api 新增通用 filterList + getBarCommentList + activityEnded；修复旧页 pager.limit/pager.total 不存在的分页 bug(统一 size/count)
- 构建通过，commit 9da2d66 + tag v1.0.37 已推送

## v1.0.38 财务管理四大页面重构
- 财务概况：全部金额改为由订单 mock 实时计算（不硬编码），展示订单总金额/有效订单总金额/退款订单总金额；托管·膳食·陪诊三类各自的总金额/有效/已取消；待结算与已结算总金额及三类分账；员工已获得收益总额
  - 口径：有效订单=已支付且未取消(status≠4)；待结算=有效且未完成；已结算=有效且已完成(status=3)
- 订单流水(finance/flow)：流水号/服务名称/昵称/手机号搜索 + 订单类型/支付方式/提交时间/支付时间筛选；字段=流水号、订单类型、服务名称、头像昵称、手机号、订单金额、支付金额、提交时间、支付时间、支付方式；新增「流水导出」
- 账单结算(finance/bill)：账单号/昵称/手机号搜索 + 账单状态筛选；字段=账单号、账单金额、用户头像昵称手机号、订单数、账单更新时间、账单结算时间、账单状态(待结算/已结算)
  - 账单详情弹窗含订单明细(托管单+膳食单)；导出支持两种：多选「账单记录表」与单条「订单明细表」；待结算账单可一键结算
- 员工收益(finance/earnings)：员工名称/手机号搜索 + 角色/添加时间筛选；字段=员工名称、手机号、角色、单次收益总金额、陪诊收益、配送收益、托管收益、添加时间
  - 添加收益弹窗：员工下拉支持名称/手机号过滤搜索，分别输入三类收益；收益导出
- 新增 src/utils/export.ts（CSV 导出，带 BOM 防 Excel 中文乱码）；mock 新增 financeFlow(从三张订单表派生)、financeBill(8条含明细)、staffEarnings(由 staffList 派生)
- 构建通过，commit 10df8b8 + tag v1.0.38 已推送

## v1.0.39 角色管理新增权限配置
- 新增 src/mock/perms.ts：权限目录 permModules，严格按各页面「实际存在的操作」生成；页面节点即「查看」权限(key=perms/lists)，子节点为操作权限
- 覆盖 9 大板块 30 个页面：数据台(3页纯查看)、小区管理(小区列表/楼栋房号/认证列表/小区详情)、用户管理、员工管理、服务管理(托管/陪诊/膳食)、订单管理(托管/膳食/陪诊，含派单)、内容管理(7页，含审核/评论/停止报名/报名列表/标记已处理)、财务管理(含导出/结算/添加收益)、系统设置(含分配权限)
- 无此功能的板块不出现对应权限：如数据台/财务概况/用户详情无任何操作权限；生活帮手仅有详情+标记已处理；认证列表仅有详情+审核
- 新增 src/views/setting/role/auth.vue 分配权限弹窗（el-tree 三级：板块→页面→操作，支持展开折叠、全选、父子联动，只保存叶子节点权限）
- setting/role/index.vue 新增「分配权限」按钮与「权限数量」列；mock/api 新增 getPermTree/getRolePerms/saveRolePerms；rolePerms 预置 4 个角色的权限
- 构建通过，commit 4c04d2b + tag v1.0.39 已推送

## v1.0.40 品牌LOGO替换
- AI 生成顾好家品牌 LOGO（薄荷绿房子+珊瑚橙爱心+"顾好家"文字，扁平风）存为 src/assets/images/ghj-logo.png
- mock/config.ts：web_favicon/web_logo/login_logo/login_image 全部指向本地 ghj-logo.png（原 picsum 随机图），登录页广告图与侧边栏 LOGO 统一为品牌 LOGO
- 构建通过，commit f33e72b + tag v1.0.40 已推送

## v1.0.41 显示/隐藏改为状态栏开关
- 8 个页面：人力资源、社区贴吧、精彩内容、社区通知、社区活动、Banner图、托管服务、膳食服务(套餐组合)
- 「显示状态/托管状态/状态」列由彩色标签改为 el-switch 开关（:model-value + change 回调避免二次翻转）；操作栏移除「显示/隐藏」文字按钮并收窄列宽
- 社区活动/贴吧/人力原在标题后追加的「已隐藏」标签移除，改为独立显示状态开关列；贴吧(audit!==1)与人力(审核未通过)开关置灰禁用
- 菜品 Tab 本就是开关；详情弹窗内只读状态标签保留。构建通过，commit ba19ffe + tag v1.0.41 已推送

## v1.0.42 列表布局统一优化
- 全局样式 src/styles/public.scss：.el-table .cell 设为 white-space: nowrap（EP 默认 normal 会换行），padding 12px->10px，表头同步 nowrap → 所有列表内容保持一行显示
- 脚本批量调整 33 个列表页列宽：收窄空泛列（时间 170/180->160、手机/电话 130/140->120、排序 90->80、ID 90->80、状态 100->90 / 110->100）
- 操作列按按钮实际文本宽度（v-if/v-else-if 分支取最大）重算：人力资源 210->270、贴吧 250->270、社区活动 290->350、社区小区 210->240、账单结算 220->240 等加宽；多余空间的（膳食/托管/订单/员工等 150->130、Banner 170->130）收窄
- 24 处纯文本列补充 show-overflow-tooltip，截断可悬浮查看完整内容
- 构建通过，commit 684599c + tag v1.0.42 已推送

## v1.0.43 图片上传入口统一为上传组件
- 新增 src/components/image-upload/index.vue：单图上传组件（v-model 字符串；虚线加号空态、缩略图 hover 蒙层「重新上传/删除」、点击图 el-image 预览；本地 FileReader 读 base64，>1MB 自动 canvas 压缩最长边 1200；校验类型与 5MB 上限）
- 替换 5 处图片链接输入：社区活动封面图、精彩内容封面图、Banner 图、陪诊服务封面图、膳食菜品封面图
- 必填项改为上传校验并移除 picsum 随机图兜底：社区活动「请上传活动封面图」、精彩内容「请上传封面图」、Banner「请上传Banner图」；陪诊/菜品保留原兜底
- 保留：员工头像（已是上传按钮+本地读取）、material-picker 场景（网站设置/头像/二维码/图标等本就是上传组件，非链接输入）
- 构建通过，commit e988f1d + tag v1.0.43 已推送

## v1.0.44 新增小区弹窗调整
- 小区门头照：默认为空，改用 ImageUpload 上传组件（原「随机图+更换门头图按钮」删除，randomCover 函数移除），非必填
- 列表门头照列：空时显示「暂无门头照」占位
- 状态文案：删除「营业中」，弹窗 switch 与列表/详情统一改为「启用/已停用」（community/lists + community/detail）
- 构建通过，已推送

## v1.0.45 小区详情删除维护状态
- community/detail 楼栋信息 Tab 移除「维护状态」列（已维护/待完善标签）
- 构建通过，已推送

## v1.0.46 房号列表新增认证状态筛选
- community/building/index.vue：标题行右侧新增「认证状态」下拉（全部状态/已认证/未认证，width 120）
- filteredRooms 由「仅按单元过滤」扩展为「单元 + 认证状态」两级过滤，certFilter ''(全部)/'1'已认证/'0'未认证
- 构建通过，commit 109126b + tag v1.0.46 已推送

## v1.0.47 用户详情删除提示语
- consumer/detail/index.vue 基本信息板底部删除 el-alert 提示「物业后台仅可查看用户账号状态，如需启用/禁用操作，请联系平台管理员处理。」
- 构建通过，commit bdb1f34 + tag v1.0.47 已推送

## v1.0.48 用户详情帖子发布删除所属板块列
- consumer/detail/index.vue 帖子发布 Tab 删除「所属板块」列（prop=board），其余列不变
- 构建通过，commit 1eb2120 + tag v1.0.48 已推送

## v1.0.49 陪诊服务删除服务项目列表
- service/escort/index.vue 整页重写：删除「陪诊服务项目」卡片（封面/名称/价格/描述/排序/状态表格 + 新增/编辑弹窗 + 相关函数与未用 import），仅保留顶部「陪诊服务单价」设置卡片
- mock/perms.ts 同步：陪诊服务权限操作删除 新增服务/编辑/删除，仅保留「单价设置」；rolePerms 预置中删除 'service.escort/edit'
- getEscortServiceList/escortServices mock 数据保留未删（无页面引用）
- 构建通过，commit f5b3112 + tag v1.0.49 已推送

## v1.0.50 分配权限弹窗精简
- setting/role/auth.vue：删除顶部 el-alert 提示条（勾选页面节点即授予查看权限...）与树节点后的「页面」「操作」el-tag 标签，节点仅显示名称
- 构建通过，commit 65ef7c2 + tag v1.0.50 已推送
