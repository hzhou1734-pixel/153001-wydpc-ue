# 2026-09-20 工作日志

## v1.0.82 托管订单托管类型多选显示（commit cf29f49，tag v1.0.82，已推送 dev 首次成功）
- 需求原文：「托管类型是多选，接、送、用餐、托管」
- `src/mock/data.ts` nursingOrders 8 条 type 由单值字符串改为数组：
  - 1001 TG20260908001 张伟 → ['托管','用餐']
  - 1002 TG20260907002 李娜 → ['接','送']
  - 1003 TG20260906003 陈晨 → ['托管']
  - 1004 TG20260905004 刘洋 → ['送']
  - 1005 TG20260904005 王强 → ['用餐','托管']
  - 1006 TG20260903006 赵敏 → ['托管']
  - 1007 TG20260902007 张伟 → ['接','送']
  - 1008 TG20260901008 李娜 → ['送','用餐','托管']（3 标签演示行）
- 编辑锚点：`sn: 'TG...', service: '...', type: '...'` 三段组合唯一（sn 也出现在 data_order.ts 评价表，但那里无 type 字段，锚点安全）
- `src/views/order/nursing/index.vue`：
  - 列表「托管类型」列改 `v-for="t in row.type"` 多标签（typeTag 四色映射复用），width 120→170
  - 详情「托管类型」descriptions-item 改同款多标签
  - 导出 CSV 该列改 formatter `(row.type || []).join('、')`（数组直接进 prop 会输出逗号拼接）
- 构建：node vite.js build 成功（1m 24s）；推送 PowerShell 重试循环 10×20s 首次即成功
- 探查结论：nursing 页 .type 仅 2 处 UI 引用（列表/详情），导出 1 处；data_order.ts 的评价 sn 复用不影响

## v1.0.83 订单详情删除优惠金额与实付金额
- 需求原文：「所有订单中删除优惠金额核实付金额」（= 和实付金额）
- 全局排查：src 下「优惠金额」仅 2 处（nursing L160 / meal L178），「实付金额」仅 1 处（nursing L164）；escort/helper 详情无这两项，无需改
- nursing/index.vue 金额信息删 2 项 → 余：托管单价/订单金额/支付方式/支付状态；meal/index.vue 删 1 项 → 余：套餐单价/购买份数/订单金额/支付方式/支付时间
- 教训：Bash 工具里 `A && VAR="x" && "$VAR" ...` 这种含变量赋值的 && 链会被 shim SIGTERM 且 stdout 全空（本次 git add/commit 均未执行）；git 固定套路必须用分号分隔（`VAR="..."; "$GITEXE" ...`）
- commit v1.0.83（tag v1.0.83），memory 文件一并纳入提交

## v1.0.84 订单支付方式口径调整
- 需求原文：「托管单、膳食单的支付方式只有顾好家币，陪诊单核生活榜首订单的支付方式只有微信支付或支付宝支付」（核=和，榜首=帮手）
- data.ts（node 脚本按 sn 前缀批量替换 + 计数校验 9/4/9/4/2 全中）：
  - TG 托管 6 条已支付 + SC 膳食 7 条已支付：pay_type → '顾好家币'，日志「微信/支付宝支付成功 ¥X」→「顾好家币支付成功 ¥X」（共 13 条 pay_type、13 条日志）
  - PZ 陪诊 2 条 '支付宝' → '支付宝支付'（微信支付行不动）；helperOrders 无 pay_type 字段无需改
- escort/index.vue：payTypeOptions ['微信支付','支付宝','银行卡'] → ['微信支付','支付宝支付']
- finance/flow/index.vue（订单流水筛选）：选项改 微信支付/支付宝支付/顾好家币（原「支付宝」值已无数据匹配，因流水 pay_type 派生自订单数组会自动同步为顾好家币/支付宝支付）
- 教训补充：Bash shim 对 && 链中含 `VAR="..."; "$VAR"` 组合的兼容性不稳，node -e 多行脚本（含换行）反而稳定；git 链式操作仍用分号分隔模式

## v1.0.85 托管订单接送信息去掉时间
- 需求原文：「这个需要接送后面不要显示时间」（托管订单派单弹窗截图）
- nursing/index.vue 两处（pickup_time 字段仅剩 mock 存储，UI 不再展示）：
  - 派单弹窗「是否需要接送」：`需要接送（${pickup_time}）` → `需要接送`（无需接送不变）
  - 详情「是否需要接送」：`需要（${pickup_time}）` → `需要`（不需要不变）
- 全局确认：pickup_time/是否需要接送 仅 nursing 页 2 处 UI 引用，其他订单页无此展示

## v1.0.86 全站筛选下拉框 placeholder 统一为「请选择」
- 需求原文：「所有的筛选状态里面都需要显示：请选择」（托管订单筛选栏截图，红框含 订单状态/支付状态/下单时间）
- node 脚本按「行含 <el-select 且 v-model 为 queryParams./searchParams.」批量处理 28 个文件 43 处：
  - 已有 placeholder（全部状态/全部/全部楼栋/全部方式/全部角色/全部星级/全部小区/全部类目/全部类型等）→ 统一替换为 请选择
  - 无 placeholder 的 10 处（article/lists cid/is_show、organization post/department、finance refund_record/refund 充值记录 pay_way/pay_status、setting dict type/data）→ 在行尾 > 前插入 placeholder="请选择"
- 范围界定：只动列表筛选 select；弹窗表单 select（已是请选择XX）不动；permission/* 旧代码不动；decoration/dev_tools 不动
- 新增列表页约定：筛选下拉 placeholder 一律 placeholder="请选择"（后续如加筛选框沿用）

## v1.0.87 托管订单地址信息上移并分接/送地址
- 需求原文：「托管单的地址信息，需要放在需要接、送底下，分别对应需要接的地址和需要送的地址」
- nursing/index.vue 详情弹窗调整：
  - 区块顺序 托管信息→支付人信息→地址信息 改为 托管信息→地址信息→支付人信息（地址信息紧跟托管类型的接/送标签之下）
  - 原「详细地址」单项改为条件渲染：type 含接且含送 → 「接的地址」+「送的地址」两项各占一列；仅含接或仅含送 → 对应单项 span=2；都不含（纯托管/用餐）→ 保留「详细地址」span=2
  - 接的地址/送的地址当前都取 detailRow.address（上门接/送回家，语义一致）；新增 hasType(t) 辅助函数（(detailRow.value?.type||[]).includes(t)）
- 派单弹窗未动（如需在派单时显示接/送地址待用户提出）

## v1.0.88 膳食订单详情删除所属小区
- 需求原文：「膳食订单详情中删除所属小区信息」
- meal/index.vue 详情支付人信息区删除「所属小区」descriptions-item（L154）；派单 staffOptions 的 community 过滤逻辑（L292-293）不受影响保留
- 其他订单详情（托管/陪诊/生活帮手）是否也删所属小区待用户指示，未动

## v1.0.89 生活帮手订单关联弹窗显示历史评分
- 需求原文：「生活帮手订单-订单关联，弹窗中需要显示接单人历史订单评分信息」
- helper/index.vue 订单关联弹窗（人才库选择表）新增「历史评分」列：el-rate disabled allow-half size=small + 橙色数字分值（talentList.stars 字段 '4.9' 等，8 人都有），列位于认证技能与接单量之间，width 160
- 弹窗宽度 720→820px 容纳新列（原列合计已近 720）


## v1.0.90 修复筛选下拉 placeholder 不显示（塌陷根因定位）
- 需求原文（第二次反馈）：「所有的筛选项中，不要显示为空，全部显示：请选择」——v1.0.86 只补了 placeholder 文本未解决渲染
- **根因（headless Edge 实测 + 源码确认）**：EP 组件样式经 unplugin/style-import 按组件后加载，晚于 main.ts 的 styles/index.scss（内含 tailwind），同权重下 EP 的 `.el-select{width:100%}` 覆盖 Tailwind 非 important 宽度类（w-[140px] 等）；在 el-form inline 收缩容器中 width:100% 循环依赖退化为 auto，而新 select 的 placeholder 是 absolute 定位不占宽、selection 是 flex:1 1 0%，整体 min-content 塌陷为 44px（仅 caret+padding），placeholder width:100%×0 → 不可见
- **修复**：① element.scss 在 :root 内给 .el-select 加 min-width:100px 兜底（min-width 不受加载顺序影响）；② 6 个文件 11 处筛选 el-select 的 class="w-[Npx]" 批量加 ! 前缀恢复设计宽度（consumer/lists 1、order/nursing 2、order/meal 2、order/helper 1、order/evaluate 2、order/escort 3；node 脚本计数校验 11/11 全中）
- **验证**：headless Edge 重跑 /order/escort，三个筛选 placeholder rect.w 从 0 → 96/76/86（=140/120/130 减去 padding），文本「请选择」灰色可见；vite build ✓ built in 56.54s
- 教训：本项目给 EP 组件写 Tailwind 宽度类必须用 ! 前缀（或依赖 min-width 兜底）；对话框水平表单不塌陷（content 宽度确定），仅 el-form inline 筛选栏受影响
- 诊断脚本沉淀在 workspace：.tmp-select-check.mjs（playwright-core + headless Edge channel msedge，admin/admin 登录）
- **推送补充（v1.0.90 实测）**：PS 通道 push 持续报 remote-https is not a git command（PATH 前置 mingw64/bin 也无效），改用 Bash 通道 + GIT_EXEC_PATH 环境变量一次成功；token 用 node 生成 Basic 头写 .tmp-push-b64.txt，bash 内通过运行时替换读取（规避安全过滤且不依赖 base64/cat）；远程 dev 已到 f903b21，tag v1.0.90 已推

## v1.0.91 列表列宽统一按比例分布（间距一致）
- 需求原文：「所有的列表数据，需要合理排布，每个字段之间相互间隔保持一致，避免间距过小或者过大」（附陪诊订单列表截图）
- 根因：数据列用固定 width 时剩余空间全部挤给表内唯一 min-width 列（如陪诊表的下单人）或在表尾留白，列间距忽大忽小
- 方案①：26 个业务视图文件 195 处数据列 width→min-width（node 脚本行级替换），剩余空间按 min-width 比例分配 → 间距一致；操作/选择/序号/展开列保持固定 width；排除未接入菜单的旧代码（permission/dev_tools/channel/setting-system/setting-dict/article-column/article-lists）
- 方案②：时间列 min-width 统一 160→170（datetime ~170px 防省略）；针对性收窄 7 个页面 31 处「取值明显短于列宽」的列（nursing 所属楼栋 150→100、building 认证业主 240→120、meal 楼栋 150→100 等，脚本计数校验 31/31）
- 验证：headless Edge（workspace .tmp-table-check.mjs，登录+列宽测量+截图）——escort 1407px 表格列宽 203/175/140/128/105/199/199/128/130 按比例铺满无表尾留白；nursing min 总和 1430→1320 后按比例 +87px 富余无横向滚动；vite build ✓ built in 49.23s
- 教训：新建列表页数据列直接写 min-width；固定 width 只给操作类列，否则布局随窗口宽度失衡
- 推送收尾：挂起的重试任务（10次×15s，第10次 git 调用挂起>6分钟）TaskStop 后，给脚本加 execFileSync timeout:90000 + 30s 间隔 ×20 次重跑 → 第 1 次尝试即成功（代理恢复），dev + tag v1.0.91 均已上 GitHub；推送后清理 .tmp-push-retry.mjs / .tmp-push-b64.txt / .tmp-push-result.txt（保留验证截图与 workspace 两个诊断脚本）

## v1.0.92 钱袋子收支明细化
- 需求：钱袋子「上传的收支明细记录」改为多条明细记录，每条单独编辑明细名称、金额、上传凭证图片
- 数据层（data_finance.ts）：新增 `export interface WalletItem { id, name, type: 1|2(收入/支出), amount, voucher }`；walletProperty×4 / walletNursing×3 / walletMeal×3 每条记录挂 items 数组（2~6 条，金额与原汇总精确一致）；buildPropertyWallet/buildBusinessWallet 改为从 items 汇总 income/expense/balance 与 prepay/cost/unit_price（结余=收入-支出，不再手工填写）
- 页面（wallet/index.vue 重写）：三列表删凭证列、加「收支明细 N 条」列 + 操作列「编辑明细」；添加弹窗金额/凭证表单项 → 动态明细行列表（名称 input / 类型 select / 金额 input-number / ImageUpload(104×72) / 删除 + 添加明细按钮，明细行 flex items-center）；新增「编辑明细」弹窗（标题带记录标题+月份、汇总行、明细行行内编辑、深拷贝副本保存时写回并重汇总）；校验 validateItems（名称/金额>0/凭证必传，按图片上传约定提示「第 N 条明细请上传凭证图片」）
- 权限：perms.ts wallet ops 加 OP_EDIT('编辑明细') + adminPerms 加 finance.wallet/edit
- 验证：vite build ✓ 1m30s（Bash 通道；PowerShell 通道连续两次把 build 进程截断在 transforming 阶段，build 改用 Bash 通道跑）；headless Edge（workspace .tmp-wallet-check.mjs）全链路通过：列表表头含收支明细/操作、编辑弹窗 6 条明细、改首条收入 26800→26801.5 保存后列表收入 ¥35601.50 自动联动、提示「明细已保存」、弹窗自动关闭
- 事故与修复：add -A 误把 .tmp-push-b64.txt（含 token）与 2 张验证截图提交进 44bbba7 → 未 push 前 git rm --cached + commit --amend + tag 重打，最终提交 e85d3e4 干净（5 files）；.gitignore 增加 `.tmp-*` 通配规则根治
- 教训：push 前必查 `git show --stat HEAD` 确认无 .tmp 文件混入；认证头临时文件生成后立即被 add -A 收录是高危动作

## v1.0.93 陪诊计费统一为半天/整天
- 需求：陪诊服务按半天价格/整天价格计算，没有按小时计费；订单里的计费方式同步修改
- 数据（data.ts，node 补丁脚本 CRLF 兼容块/行双模式替换，count 校验全中）：escortPriceSetting hour_price/min_hours → half_price 128 / day_price 220；data.ts 旧 escortServices 去「（时价）」改 half_price/day_price 结构；escortOrders×8 的 hours/hour_price → charge_type('半天'|'整天') + price，amount=price，服务名与 data_service.ts 同源（标准陪诊服务 128/220、专家门诊陪诊 仅半天 198、老人专属陪诊 仅整天 268），logs 支付文案同步
- 联动：data_user.ts 用户订单历史 2 条服务名/金额同步；data_order.ts 陪诊评价 2 条标题按 sn 对应订单同步；api.ts 注释更新（接口无页面引用）；order/escort/index.vue 列表加「计费方式」列（整天 warning / 半天 info tag），详情两处「陪诊时长/时薪标准」→「计费方式/服务单价」
- 保留：人才库/生活帮手的保洁、家教描述「按小时计费」与陪诊无关，未动
- 验证：vite build ✓ 1m34s；headless Edge（workspace .tmp-escort-check.mjs）——列表表头含计费方式、首行 半天/¥198.00、详情 label 无时长/时薪、服务页半天/整天价列正常
- 坑：node 补丁脚本 block 替换需兼容 CRLF（\n 匹配失败 x0 时改用 \r\n 重试）
- 推送：首轮 2 次手动 + 后台 20 次×30s 重试中第 15 次成功（代理 502/schannel 间歇约 10 分钟），dev + tag v1.0.93 均已上 GitHub；重试脚本成功后自清理（retry 脚本/result/b64 均无残留）

## v1.0.94 陪诊订单详情微调
- 删除详情弹窗「所属小区」项（用户说「所属校区」实指所属小区）；附加费用表格新增「凭证图片」列（el-image 缩略 + 点击预览）
- data_order.ts escortExtraFees 每条记录加 voucher 字段（picsum seed ghj-extra-30XX，6 单 9 条费用全覆盖）；3005/3007 无附加费用仍显示「无附加费用」
- 验证：build ✓ 58.95s；headless Edge——详情 label 无所属小区、附加费用表头含凭证图片、首单 2 张凭证图

## v1.0.95 膳食订单三处调整
- 用户指令：列表表头删除「支付方式」列；详情「是否需要配送」右侧移除送达时间数据；支付人信息删除「与下单人关系」
- 修改仅 src/views/order/meal/index.vue 三处：列表删支付方式列；详情 need_delivery 渲染 `需要（${delivery_time}）` → `需要`；删 payer_relation 描述项
- mock 数据字段（delivery_time/payer_relation）保留未删（nursing/escort 详情仍用 payer_relation；派单弹窗「配送要求」用户未提及，保留）
- 验证：vite build ✓ 1m07s（Bash 通道；本轮 Bash coreutils 缺失 tail/grep 无法用，全改 node 读写检查）；headless Edge——表头 11 列无支付方式、详情 label 无与下单人关系、deliveryValue="需要"
- 顺带清理项目内历史 .tmp-* 残留（构建日志/旧截图/req 等，均已在 .gitignore 通配内）

## v1.0.96 托管订单详情删除所属小区
- 用户指令：托管订单中删除所属小区字段——仅 src/views/order/nursing/index.vue 详情支付人信息区删 descriptions-item（与 v1.0.94 陪诊同口径）
- 保留：列表/导出本就无小区列（只有文档口径的所属楼栋 building）；派单 staffOptions 按 row.community 过滤员工的逻辑依赖未动；mock 字段未删；「与下单人关系」用户未提及保留
- 验证：vite build ✓ 1m04s；headless Edge——详情 labels 无所属小区（hasCommunity:false），支付人信息剩 5 项

## v1.0.97 托管服务删除托管类型、改四项单价
- 用户指令：添加/编辑托管服务删除「托管类型」选项，补充 接/送/用餐/托管 4 项单价
- data_service.ts：NursingItem 删 type，price 拆为 price_pickup/price_send/price_meal/price_care（0=不提供）；删 nursingTypeOptions；8 条样例数据按原价拆分（如 1001 原 188 → 15/15/25/133）
- service/nursing/index.vue：筛选区删托管类型下拉及过滤逻辑；列表价格列改「单价（接 / 送 / 用餐 / 托管）」一行四价；弹窗删类型选择、价格改 4 个 input-number；editForm/openAdd/openEdit/submitEdit 同步
- banner/index.vue：托管服务选项 `${s.name}（${s.type}）` → `${s.name}`（type 已删）
- 保留：data.ts 另一份 nursingServices（旧接口数据无页面引用）与托管订单数据未动
- 验证：vite build ✓ 56.76s；headless Edge——列表表头含「单价（接 / 送 / 用餐 / 托管）」、首行 接¥15.00/送¥15.00/用餐¥25.00/托管¥133.00、筛选区无托管类型、编辑与添加弹窗 labels 为 标题+四单价+描述+排序+状态、banner 页无 undefined
- 坑复现：node -e 清理脚本又遇 SIGTERM exit 1，原样重跑成功

## v1.0.98 膳食服务早餐/午餐/晚餐改 tab 切换
- 用户指令（附截图）：膳食服务把早餐、午餐、晚餐放在顶部位置做 tab 切换
- service/meal/index.vue：原三个上下排列的餐段区块（el-card v-for）改为「今日/明日」tabs 下的第二层 el-tabs（早餐/午餐/晚餐），添加按钮移至餐段 tab 行右侧，表格只渲染当前餐段（currentMealList computed）；openEdit/delRow 改传 activeMeal
- 保留：今日/明日菜单维度、空态文案、弹窗逻辑不变
- 验证：vite build ✓ 1m09s；headless Edge——两组 tabs 正常、单表格、早餐4行/午餐5行/晚餐3行切换正确、添加按钮存在

## v1.0.99 膳食订单送达照片仅一张
- 用户指令：膳食订单的送达照片只有一张
- data.ts mealOrders：2002/2004 的 delivery_imgs 各删 1 张（原 2 张→1 张），2008 本就 1 张
- order/meal/index.vue 详情「送达确认」：多图 v-for 循环改单图（delivery_imgs[0]），点击预览保留
- 验证：build ✓（日志输出 built in 1m——整分钟无秒后缀，BUILT 正则再次漏报，实际成功）；headless Edge 三单详情送达确认图数均为 1

## main 分支全量同步（2026-09-20 用户指令）
- 要求：同步所有内容到 GitHub main 主分支，确保全部推送上去
- 前置检查：工作区干净；dev 领先 origin/main 103 提交（v1.0.57~v1.0.99 全部），origin/main 无 dev 缺失提交（dev..origin/main=0），main 为 dev 祖先 → 纯 fast-forward
- 执行：push origin dev:main（6c63543..bcc84bb，代理间歇前 5 次失败第 6 次成功）+ push origin --tags（Everything up-to-date，v1.0.0~v1.0.99 共 100 个 tag 此前均已随版本推送）+ ls-remote 验证 origin/main=bcc84bb 且 v1.0.99 tag 在远程
- MEMORY.md 更新 main 同步状态与后续同步套路（push origin dev:main + --tags）
