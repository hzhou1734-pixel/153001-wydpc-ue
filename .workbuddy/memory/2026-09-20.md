# 2026-09-20 工作日志

## v1.0.82 托管订单托管类型多选显示（commit cf29f49，tag v1.0.82，已推送 dev 首次成功）
- 需求原文：「托管类型是多选，接、送、用餐、托管」
- `src/mock/data.ts` nursingOrders 8 条 type 由单值字符串改为数组：
  - 1001 TG20260908001 张伟 → ['托管','用餐']
  - 1002 TG20260907002 李娜 → ['接','送']
  - 1003 TG20260906003 陈晨 → ['托管']
  - 1004 TG20260905004 刘洋 → ['送']
  - 1005 TG20260904005 王强 → ['用餐','托管']
  - 1006 TG20260903006 赵敏 → ['托管']
  - 1007 TG20260902007 张伟 → ['接','送']
  - 1008 TG20260901008 李娜 → ['送','用餐','托管']（3 标签演示行）
- 编辑锚点：`sn: 'TG...', service: '...', type: '...'` 三段组合唯一（sn 也出现在 data_order.ts 评价表，但那里无 type 字段，锚点安全）
- `src/views/order/nursing/index.vue`：
  - 列表「托管类型」列改 `v-for="t in row.type"` 多标签（typeTag 四色映射复用），width 120→170
  - 详情「托管类型」descriptions-item 改同款多标签
  - 导出 CSV 该列改 formatter `(row.type || []).join('、')`（数组直接进 prop 会输出逗号拼接）
- 构建：node vite.js build 成功（1m 24s）；推送 PowerShell 重试循环 10×20s 首次即成功
- 探查结论：nursing 页 .type 仅 2 处 UI 引用（列表/详情），导出 1 处；data_order.ts 的评价 sn 复用不影响

## v1.0.83 订单详情删除优惠金额与实付金额
- 需求原文：「所有订单中删除优惠金额核实付金额」（= 和实付金额）
- 全局排查：src 下「优惠金额」仅 2 处（nursing L160 / meal L178），「实付金额」仅 1 处（nursing L164）；escort/helper 详情无这两项，无需改
- nursing/index.vue 金额信息删 2 项 → 余：托管单价/订单金额/支付方式/支付状态；meal/index.vue 删 1 项 → 余：套餐单价/购买份数/订单金额/支付方式/支付时间
- 教训：Bash 工具里 `A && VAR="x" && "$VAR" ...` 这种含变量赋值的 && 链会被 shim SIGTERM 且 stdout 全空（本次 git add/commit 均未执行）；git 固定套路必须用分号分隔（`VAR="..."; "$GITEXE" ...`）
- commit v1.0.83（tag v1.0.83），memory 文件一并纳入提交

## v1.0.84 订单支付方式口径调整
- 需求原文：「托管单、膳食单的支付方式只有顾好家币，陪诊单核生活榜首订单的支付方式只有微信支付或支付宝支付」（核=和，榜首=帮手）
- data.ts（node 脚本按 sn 前缀批量替换 + 计数校验 9/4/9/4/2 全中）：
  - TG 托管 6 条已支付 + SC 膳食 7 条已支付：pay_type → '顾好家币'，日志「微信/支付宝支付成功 ¥X」→「顾好家币支付成功 ¥X」（共 13 条 pay_type、13 条日志）
  - PZ 陪诊 2 条 '支付宝' → '支付宝支付'（微信支付行不动）；helperOrders 无 pay_type 字段无需改
- escort/index.vue：payTypeOptions ['微信支付','支付宝','银行卡'] → ['微信支付','支付宝支付']
- finance/flow/index.vue（订单流水筛选）：选项改 微信支付/支付宝支付/顾好家币（原「支付宝」值已无数据匹配，因流水 pay_type 派生自订单数组会自动同步为顾好家币/支付宝支付）
- 教训补充：Bash shim 对 && 链中含 `VAR="..."; "$VAR"` 组合的兼容性不稳，node -e 多行脚本（含换行）反而稳定；git 链式操作仍用分号分隔模式

## v1.0.85 托管订单接送信息去掉时间
- 需求原文：「这个需要接送后面不要显示时间」（托管订单派单弹窗截图）
- nursing/index.vue 两处（pickup_time 字段仅剩 mock 存储，UI 不再展示）：
  - 派单弹窗「是否需要接送」：`需要接送（${pickup_time}）` → `需要接送`（无需接送不变）
  - 详情「是否需要接送」：`需要（${pickup_time}）` → `需要`（不需要不变）
- 全局确认：pickup_time/是否需要接送 仅 nursing 页 2 处 UI 引用，其他订单页无此展示
